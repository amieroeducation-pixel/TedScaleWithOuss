---
phase: 05-automatisations-cron
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/007_cron_logs.sql
  - src/lib/supabase/cron-client.ts
  - src/lib/cron/auth.ts
  - src/lib/cron/logger.ts
  - src/lib/cron/report-builder.ts
  - src/app/api/cron/weekly-report/route.ts
  - src/app/api/cron/client-health/route.ts
  - src/app/api/cron/rdv-reminder/route.ts
  - src/app/api/cron/revenue-alert/route.ts
autonomous: true
requirements:
  - AUTO-01
  - AUTO-02
  - AUTO-03
  - AUTO-04

must_haves:
  truths:
    - "GET /api/cron/weekly-report retourne 200 et envoie un email Brevo au CGP avec CA mois, alertes clients, RDV semaine (AUTO-01)"
    - "GET /api/cron/client-health retourne 200 et envoie email + SMS critique pour les clients depassant le seuil d'inactivite (AUTO-02)"
    - "GET /api/cron/rdv-reminder retourne 200 et envoie SMS (ou email fallback) pour chaque RDV planifie le lendemain (AUTO-03)"
    - "GET /api/cron/revenue-alert retourne 200 et envoie une alerte email si CA mensuel < ca_monthly_target (AUTO-04)"
    - "Un appel sans header x-cron-secret valide retourne 401 (quand CRON_SECRET est configure dans .env.local)"
    - "Chaque execution insere une ligne dans cron_logs avec job_name, status (success/error/skipped), details JSONB"
    - "Aucune route cron n'utilise cookies() ni getUser() -- elles fonctionnent via curl sans session navigateur"
  artifacts:
    - path: "supabase/migrations/007_cron_logs.sql"
      provides: "Table cron_logs avec RLS SELECT user_id, INSERT via service_role"
      contains: "CREATE TABLE cron_logs"
    - path: "src/lib/supabase/cron-client.ts"
      provides: "createSupabaseCronClient() sans dependency cookies()"
      exports: ["createSupabaseCronClient"]
    - path: "src/lib/cron/auth.ts"
      provides: "verifyCronSecret(req) -- retourne NextResponse|null"
      exports: ["verifyCronSecret"]
    - path: "src/lib/cron/logger.ts"
      provides: "logCronRun() -- INSERT cron_logs sans throw"
      exports: ["logCronRun"]
    - path: "src/lib/cron/report-builder.ts"
      provides: "buildWeeklyReportHtml() -- HTML dark/gold"
      exports: ["buildWeeklyReportHtml"]
    - path: "src/app/api/cron/weekly-report/route.ts"
      provides: "GET handler AUTO-01"
      exports: ["GET"]
    - path: "src/app/api/cron/client-health/route.ts"
      provides: "GET handler AUTO-02"
      exports: ["GET"]
    - path: "src/app/api/cron/rdv-reminder/route.ts"
      provides: "GET handler AUTO-03 avec fallback SMS si WhatsApp non configure"
      exports: ["GET"]
    - path: "src/app/api/cron/revenue-alert/route.ts"
      provides: "GET handler AUTO-04 (alerte apres le 15 du mois seulement)"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/cron/*/route.ts"
      to: "src/lib/supabase/cron-client.ts"
      via: "createSupabaseCronClient() -- service_role sans cookies"
      pattern: "createSupabaseCronClient"
    - from: "src/app/api/cron/*/route.ts"
      to: "src/lib/sequences/brevo.ts"
      via: "sendBrevoEmail() / sendBrevoSms()"
      pattern: "sendBrevoEmail|sendBrevoSms"
    - from: "src/app/api/cron/*/route.ts"
      to: "supabase cron_logs table"
      via: "logCronRun()"
      pattern: "logCronRun"
---

<objective>
Implementer l'infrastructure complete des 4 automatisations cron : migration BDD, 4 helpers partages (cron-client, auth, logger, report-builder), et 4 Route Handlers Next.js couvrant AUTO-01 a AUTO-04.

Purpose: Les 4 automatisations fonctionnent de bout en bout -- appel curl depuis Task Scheduler = email/SMS envoye via Brevo + trace dans cron_logs. Le plan 05-02 branchera la page /automatisations sur ces logs reels.

Output:
- supabase/migrations/007_cron_logs.sql : table cron_logs avec RLS
- src/lib/supabase/cron-client.ts : client Supabase service_role sans cookies
- src/lib/cron/{auth,logger,report-builder}.ts : helpers partages
- src/app/api/cron/{weekly-report,client-health,rdv-reminder,revenue-alert}/route.ts : 4 endpoints
</objective>

<execution_context>
@C:\Users\Ted\Documents\GitHub\TedScaleWithOuss\.planning\workflows\execute-plan.md
</execution_context>

<context>
@C:\Users\Ted\Documents\GitHub\TedScaleWithOuss\.planning\ROADMAP.md
@C:\Users\Ted\Documents\GitHub\TedScaleWithOuss\.planning\REQUIREMENTS.md
@C:\Users\Ted\Documents\GitHub\TedScaleWithOuss\.planning\phases\05-automatisations-cron\05-RESEARCH.md

<interfaces>
<!-- Helpers existants -- reutiliser directement, ne pas reimplementer -->

Depuis src/lib/sequences/brevo.ts:
```typescript
type BrevoResult = { success: boolean; error?: string }

export async function sendBrevoEmail(args: {
  to: string
  toName: string
  subject: string
  htmlContent: string
}): Promise<BrevoResult>

export async function sendBrevoSms(args: {
  to: string  // E.164 ex: +33612345678
  content: string
  sender?: string
}): Promise<BrevoResult>
// Lit process.env.BREVO_API_KEY et process.env.BREVO_SENDER_EMAIL
// Retourne { success: false, error: '...' } si cles absentes -- ne throw pas
```

Depuis src/lib/api.ts:
```typescript
export function apiSuccess<T>(data: T, status?: number): NextResponse
export function apiError(message: string, status?: number): NextResponse
// apiError logue deja avec console.error -- ne pas logger en double
```

CRITIQUE -- src/lib/supabase/server.ts:
```typescript
// createSupabaseServiceClient() appelle cookies() de next/headers
// => ne fonctionne PAS dans les routes cron appelees par curl (pas de cookie store)
// => Creer createSupabaseCronClient() dans src/lib/supabase/cron-client.ts sans cookies
export async function createSupabaseServiceClient()  // NE PAS UTILISER dans les routes cron
```

RPC client health (depuis src/app/api/clients/health/route.ts):
```typescript
const { data } = await supabase.rpc('get_client_health_alerts', { p_user_id: userId })
// Retourne Array<{ client_id, prospect_id, full_name, last_interaction_at,
//   days_without_contact, alert_threshold_days, total_aum }>
```

Vue revenue (depuis src/app/api/revenue/stats/route.ts):
```typescript
supabase.from('v_monthly_revenue')
  .select('month_num, revenue, objective, pct_of_objective')
  .eq('user_id', userId)
  .eq('year', currentYear)
// revenue et objective sont des nombres -- Number(row.revenue)
```

RDV J-1 (depuis RESEARCH.md -- interactions table):
```typescript
import { addDays, startOfDay, endOfDay } from 'date-fns'
const tomorrow = addDays(new Date(), 1)
supabase.from('interactions')
  .select('id, type, occurred_at, prospects(full_name, phone, phone_normalized, email)')
  .eq('user_id', userId)
  .in('type', ['rdv1', 'rdv2', 'rdv3'])
  .gte('occurred_at', startOfDay(tomorrow).toISOString())
  .lte('occurred_at', endOfDay(tomorrow).toISOString())
```

Email destinataire CGP (pitfall 4 -- pas dans user_settings):
```typescript
// auth.users.email n'est pas dans user_settings -- utiliser admin API
const { data: authData } = await supabase.auth.admin.getUserById(userId)
const recipientEmail = authData?.user?.email ?? ''
```

user_settings colonnes pertinentes:
- ca_monthly_target: number | null (defaut 15000)
- client_health_threshold_days: number | null (defaut 90)
- brevo_api_key: ignorer -- utiliser process.env.BREVO_API_KEY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : Migration 007 + 4 helpers cron (cron-client, auth, logger, report-builder)</name>
  <files>
    supabase/migrations/007_cron_logs.sql
    src/lib/supabase/cron-client.ts
    src/lib/cron/auth.ts
    src/lib/cron/logger.ts
    src/lib/cron/report-builder.ts
  </files>
  <action>
**1. supabase/migrations/007_cron_logs.sql**

```sql
-- Migration 007: table cron_logs pour historique executions automatisations
CREATE TABLE cron_logs (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  job_name text NOT NULL,
  status text NOT NULL CHECK (status IN ('success', 'error', 'skipped')),
  details jsonb DEFAULT '{}',
  executed_at timestamptz DEFAULT now() NOT NULL
);

CREATE INDEX idx_cron_logs_user ON cron_logs(user_id);
CREATE INDEX idx_cron_logs_job ON cron_logs(job_name, executed_at DESC);

ALTER TABLE cron_logs ENABLE ROW LEVEL SECURITY;

-- Lecture: chaque user voit ses propres logs (page /automatisations)
CREATE POLICY "Users can view their own cron_logs" ON cron_logs
  FOR SELECT USING (auth.uid() = user_id);
-- INSERT via service_role (bypass RLS) depuis les routes cron -- pas de policy INSERT
```

**2. src/lib/supabase/cron-client.ts**

Ne pas utiliser createServerClient de @supabase/ssr (depend de cookies).
Utiliser createClient de @supabase/supabase-js directement :

```typescript
import { createClient } from '@supabase/supabase-js'

export function createSupabaseCronClient() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false } }
  )
}
```

Pas d'async, pas de cookies(). Retourne un SupabaseClient synchrone service_role qui bypass RLS.

**3. src/lib/cron/auth.ts**

```typescript
import { NextRequest } from 'next/server'
import { apiError } from '@/lib/api'

export function verifyCronSecret(req: NextRequest): ReturnType<typeof apiError> | null {
  const expected = process.env.CRON_SECRET
  if (!expected) return null  // Pas de secret configure = dev mode ouvert
  const provided = req.headers.get('x-cron-secret')
  if (provided !== expected) {
    return apiError('Cron unauthorized', 401)
  }
  return null  // OK -- continuer
}
```

**4. src/lib/cron/logger.ts**

```typescript
import { createSupabaseCronClient } from '@/lib/supabase/cron-client'

export type CronStatus = 'success' | 'error' | 'skipped'

export async function logCronRun(args: {
  userId: string
  jobName: string
  status: CronStatus
  details?: Record<string, unknown>
}): Promise<void> {
  try {
    const supabase = createSupabaseCronClient()
    await supabase.from('cron_logs').insert({
      user_id: args.userId,
      job_name: args.jobName,
      status: args.status,
      details: args.details ?? {},
    })
  } catch (e) {
    // Le log ne doit jamais faire echouer la route principale
    console.error('[logCronRun] Echec insert cron_logs:', e)
  }
}
```

**5. src/lib/cron/report-builder.ts**

Inline CSS uniquement. Pas de classes Tailwind. Couleurs dark/gold coherentes avec le projet :
- Background: #0a0a0a
- Gold: #c9a84c
- Text primaire: #e5e5e5
- Text secondaire: #999
- Vert: #4ade80 (CA >= objectif)
- Orange: #f59e0b (alerte warning)
- Rouge: #ef4444 (alerte critical)

```typescript
export function buildWeeklyReportHtml(data: {
  caWeek: number
  caMonth: number
  caObjective: number
  healthAlerts: Array<{ full_name: string; days_without_contact: number; severity: string }>
  relancesDues: number
  rdvSemaine: number
  weekLabel: string
}): string {
  const pct = data.caObjective > 0
    ? Math.round((data.caMonth / data.caObjective) * 100)
    : 0
  const pctColor = pct >= 100 ? '#4ade80' : pct >= 70 ? '#c9a84c' : '#ef4444'

  const alertsHtml = data.healthAlerts.length === 0
    ? '<p style="color:#4ade80;font-size:13px;margin:4px 0;">Aucune alerte -- Tout est OK</p>'
    : data.healthAlerts.slice(0, 10).map(a => `
        <div style="padding:8px 0;border-bottom:1px solid #1a1a1a;display:flex;gap:12px;align-items:center;">
          <span style="font-size:11px;font-weight:700;color:${a.severity === 'critical' ? '#ef4444' : '#f59e0b'};min-width:60px;">
            ${a.severity === 'critical' ? 'CRITIQUE' : 'ALERTE'}
          </span>
          <span style="color:#e5e5e5;font-size:12px;flex:1;">${a.full_name}</span>
          <span style="color:#999;font-size:11px;">${a.days_without_contact}j sans contact</span>
        </div>`).join('')

  return `<!DOCTYPE html>
<html lang="fr">
<body style="font-family:Arial,sans-serif;max-width:600px;margin:0 auto;background:#0a0a0a;color:#e5e5e5;padding:24px;">
  <h1 style="color:#c9a84c;font-size:22px;margin-bottom:4px;">Rapport Hebdomadaire -- Ted CGP</h1>
  <p style="color:#666;font-size:12px;margin-top:0;margin-bottom:20px;">${data.weekLabel}</p>

  <div style="background:#111;border:1px solid #222;border-radius:8px;padding:16px;margin-bottom:16px;">
    <h2 style="color:#c9a84c;font-size:14px;margin-top:0;margin-bottom:12px;">CA Mois en cours</h2>
    <p style="font-size:26px;font-weight:700;color:${pctColor};margin:0 0 8px;">
      ${data.caMonth.toLocaleString('fr-FR')} EUR
    </p>
    <p style="color:#999;font-size:12px;margin:0 0 4px;">
      Objectif : ${data.caObjective.toLocaleString('fr-FR')} EUR
      -- <strong style="color:${pctColor};">${pct}%</strong> atteint
    </p>
    <p style="color:#999;font-size:12px;margin:0;">
      CA cette semaine : <strong style="color:#e5e5e5;">${data.caWeek.toLocaleString('fr-FR')} EUR</strong>
    </p>
  </div>

  <div style="background:#111;border:1px solid #222;border-radius:8px;padding:16px;margin-bottom:16px;">
    <h2 style="color:#c9a84c;font-size:14px;margin-top:0;margin-bottom:12px;">Alertes Clients</h2>
    ${alertsHtml}
  </div>

  <div style="background:#111;border:1px solid #222;border-radius:8px;padding:16px;">
    <h2 style="color:#c9a84c;font-size:14px;margin-top:0;margin-bottom:8px;">Cette semaine</h2>
    <p style="color:#e5e5e5;font-size:13px;margin:4px 0;">
      ${data.relancesDues} relance(s) dues cette semaine
    </p>
    <p style="color:#e5e5e5;font-size:13px;margin:4px 0;">
      ${data.rdvSemaine} RDV planifie(s) cette semaine
    </p>
  </div>

  <p style="color:#333;font-size:10px;margin-top:24px;text-align:center;">
    Ted Scale With Ouss -- Rapport automatique chaque lundi 8h
  </p>
</body>
</html>`
}
```
  </action>
  <verify>
    <automated>cd "C:\Users\Ted\Documents\GitHub\TedScaleWithOuss" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - supabase/migrations/007_cron_logs.sql cree avec table, index, RLS, policy SELECT
    - src/lib/supabase/cron-client.ts exporte createSupabaseCronClient() sans aucun import de next/headers ou cookies
    - src/lib/cron/auth.ts exporte verifyCronSecret() qui retourne NextResponse|null
    - src/lib/cron/logger.ts exporte logCronRun() avec try/catch -- ne throw jamais
    - src/lib/cron/report-builder.ts exporte buildWeeklyReportHtml() avec HTML inline dark/gold
    - npx tsc --noEmit passe sans erreur sur ces fichiers
  </done>
</task>

<task type="auto">
  <name>Tache 2 : 4 Route Handlers cron -- weekly-report, client-health, rdv-reminder, revenue-alert</name>
  <files>
    src/app/api/cron/weekly-report/route.ts
    src/app/api/cron/client-health/route.ts
    src/app/api/cron/rdv-reminder/route.ts
    src/app/api/cron/revenue-alert/route.ts
  </files>
  <action>
**Squelette commun a toutes les routes cron :**

```typescript
import { NextRequest } from 'next/server'
import { verifyCronSecret } from '@/lib/cron/auth'
import { logCronRun } from '@/lib/cron/logger'
import { createSupabaseCronClient } from '@/lib/supabase/cron-client'
import { sendBrevoEmail, sendBrevoSms } from '@/lib/sequences/brevo'
import { apiSuccess, apiError } from '@/lib/api'

export async function GET(req: NextRequest) {
  const authError = verifyCronSecret(req)
  if (authError) return authError

  const supabase = createSupabaseCronClient()
  const { data: users, error } = await supabase.from('user_settings').select('id, ...')
  if (error) return apiError(`user_settings: ${error.message}`)

  let processed = 0
  const errors: string[] = []

  for (const user of users ?? []) {
    try {
      // ... logique metier ...
      await logCronRun({ userId: user.id, jobName: 'job-name', status: 'success', details: { ... } })
      processed++
    } catch (e) {
      const msg = e instanceof Error ? e.message : 'Erreur inconnue'
      errors.push(`user ${user.id}: ${msg}`)
      await logCronRun({ userId: user.id, jobName: 'job-name', status: 'error', details: { error: msg } })
    }
  }

  return apiSuccess({ status: 'ok', processed, errors })
}
```

---

**src/app/api/cron/weekly-report/route.ts** (AUTO-01 -- rapport lundi 8h)

Imports additionnels: `addDays, startOfDay, endOfDay, startOfWeek, endOfWeek, format` depuis `date-fns`, `fr` depuis `date-fns/locale`, `buildWeeklyReportHtml` depuis `@/lib/cron/report-builder`.

user_settings select: `'id, ca_monthly_target'`

Pour chaque user :
1. Recuperer l'email : `supabase.auth.admin.getUserById(user.id)` -> `authData?.user?.email`
   - Si absent: push dans errors, continue
2. CA mois courant: query `v_monthly_revenue` avec `.eq('user_id', user.id).eq('year', now.getFullYear())`,
   trouver le row avec `month_num === now.getMonth() + 1`, prendre `.revenue` (Number)
3. Objectif: `user.ca_monthly_target ?? 15000`
4. CA semaine: query `contracts` avec `.eq('user_id', user.id).eq('commission_status', 'percue')`
   `.gte('commission_date', startOfWeek(now, { weekStartsOn: 1 }).toISOString())`
   `.lte('commission_date', endOfWeek(now, { weekStartsOn: 1 }).toISOString())`
   Sommer `commission_amount` (Number)
5. Health alerts: `supabase.rpc('get_client_health_alerts', { p_user_id: user.id })`
   Mapper vers `{ full_name, days_without_contact, severity }` ou severity est:
   `days_without_contact >= threshold * 1.5 ? 'critical' : 'warning'`
6. Relances dues: `.from('prospects').select('*', { count: 'exact', head: true })`
   `.eq('user_id', user.id).gte('next_action_date', startOfDay(now).toISOString())`
   `.lte('next_action_date', endOfWeek(now, { weekStartsOn: 1 }).toISOString())`
7. RDV semaine: `.from('interactions').select('*', { count: 'exact', head: true })`
   `.eq('user_id', user.id).in('type', ['rdv1','rdv2','rdv3'])`
   `.gte('occurred_at', startOfWeek(now, { weekStartsOn: 1 }).toISOString())`
   `.lte('occurred_at', endOfWeek(now, { weekStartsOn: 1 }).toISOString())`
8. `weekLabel = format(startOfWeek(now, { weekStartsOn: 1 }), "d MMMM yyyy", { locale: fr })`
   Prefixer avec "Semaine du "
9. Construire HTML: `buildWeeklyReportHtml({ caWeek, caMonth, caObjective, healthAlerts, relancesDues, rdvSemaine, weekLabel })`
10. `sendBrevoEmail({ to: email, toName: 'Ted -- CGP', subject: `Rapport hebdo -- ${weekLabel}`, htmlContent })`
11. `logCronRun({ ..., jobName: 'weekly-report', status: emailResult.success ? 'success' : 'error', details: { caWeek, caMonth, caObjective, alertsCount: healthAlerts.length, emailSent: emailResult.success, error: emailResult.error } })`

---

**src/app/api/cron/client-health/route.ts** (AUTO-02 -- alertes quotidiennes)

user_settings select: `'id'`

Pour chaque user :
1. Recuperer email via `supabase.auth.admin.getUserById`
2. RPC `get_client_health_alerts(p_user_id: user.id)`
3. Si 0 alertes: `logCronRun({ ..., status: 'skipped', details: { reason: 'Aucune alerte' } })` puis continue
4. Mapper les alertes : `{ full_name, days_without_contact, severity, phone_normalized }`
   (phone_normalized peut etre dans le prospect via le join -- si absent, le SMS est skip)
5. HTML email avec liste des alertes (meme style dark/gold que report-builder) :
   Chaque ligne: nom, jours sans contact, badge CRITIQUE/ALERTE
6. `sendBrevoEmail({ to: email, toName: 'Ted -- CGP', subject: `${alerts.length} alerte(s) client health`, htmlContent })`
7. Pour les alertes critical UNIQUEMENT avec phone_normalized disponible:
   Le RPC ne retourne pas directement phone_normalized. Il faut une query complementaire:
   ```
   supabase.from('prospects')
     .select('phone_normalized')
     .in('id', criticalProspectIds)
   ```
   Puis pour chaque numero: `sendBrevoSms({ to: phone, content: `Alerte CGP: ${full_name} sans contact depuis ${days}j -- a relancer en urgence.` })`
8. `logCronRun({ ..., jobName: 'client-health', status: ..., details: { alertsCount, criticalCount, emailSent, smsSentCount } })`

---

**src/app/api/cron/rdv-reminder/route.ts** (AUTO-03 -- rappel J-1 avec WhatsApp/SMS fallback)

Imports additionnels: `addDays, startOfDay, endOfDay, format` depuis `date-fns`, `fr` depuis `date-fns/locale`.
user_settings select: `'id'`

Helper interne `sendWhatsAppMessage(phone: string, message: string)` dans le meme fichier :
- Lire `process.env.WHATSAPP_PHONE_NUMBER_ID` et `process.env.WHATSAPP_ACCESS_TOKEN`
- Si absents: return `{ success: false, error: 'WhatsApp non configure' }` (ne throw pas)
- Sinon: POST `https://graph.facebook.com/v21.0/${phoneNumberId}/messages` avec body JSON
  `{ messaging_product: 'whatsapp', to: phone.replace(/\D/g,''), type: 'text', text: { body: message } }`
  Header: `Authorization: Bearer ${accessToken}`, `Content-Type: application/json`
- Si response.ok: return `{ success: true }`
- Sinon: return `{ success: false, error: 'HTTP ...' }`

Pour chaque user :
1. `tomorrow = addDays(new Date(), 1)`
2. Query interactions pour demain (pattern depuis interfaces ci-dessus) + join `prospects(full_name, phone, phone_normalized, email)`
3. Si 0 RDV: `logCronRun({ ..., status: 'skipped', details: { reason: 'Aucun RDV demain' } })` puis continue
4. Pour chaque RDV :
   - `prospect = Array.isArray(rdv.prospects) ? rdv.prospects[0] : rdv.prospects`
   - `rdvTime = rdv.occurred_at ? format(new Date(rdv.occurred_at), 'HH:mm') : ''`
   - `message = `Rappel RDV: vous avez un rendez-vous demain ${format(tomorrow, 'EEEE d MMMM', { locale: fr })}${rdvTime ? ' a ' + rdvTime : ''}. Ted CGP.``
   - `phone = prospect?.phone_normalized || prospect?.phone`
   - Strategie envoi (par ordre de preference):
     a. Tenter WhatsApp: `sendWhatsAppMessage(phone, message)` -- si success, incrementer sentCount
     b. Sinon fallback SMS: `sendBrevoSms({ to: phone, content: message })` -- si success, incrementer sentCount
     c. Si pas de phone mais email: `sendBrevoEmail({ to: email, toName: full_name, subject: 'Rappel RDV demain', htmlContent: '<p>' + message + '</p>' })`
     d. Si rien: noter dans les erreurs
5. `logCronRun({ ..., jobName: 'rdv-reminder', status: errors.length === 0 ? 'success' : 'error', details: { rdvCount, sentCount, errors } })`

---

**src/app/api/cron/revenue-alert/route.ts** (AUTO-04 -- alerte CA < seuil)

user_settings select: `'id, ca_monthly_target'`

Pour chaque user :
1. Si `ca_monthly_target` est null ou <= 0: `logCronRun({ status: 'skipped', details: { reason: 'Pas d objectif configure' } })` puis continue
2. CA mois courant: query `v_monthly_revenue` (meme pattern que weekly-report)
3. `pct = Math.round((caMonth / user.ca_monthly_target) * 100)`
4. Conditions d'envoi (les DEUX doivent etre vraies pour envoyer):
   - `caMonth < user.ca_monthly_target` (CA sous objectif)
   - `new Date().getDate() >= 15` (on est apres le 15 du mois -- evite spam en debut de mois)
5. Si conditions non remplies: `logCronRun({ status: 'skipped', details: { reason, caMonth, target: user.ca_monthly_target, pct } })` puis continue
6. Recuperer email via `supabase.auth.admin.getUserById`
7. HTML email: titre "Alerte CA Mensuel", CA actuel en rouge, objectif, pct, ecart restant
8. `sendBrevoEmail({ to: email, toName: 'Ted -- CGP', subject: `Alerte CA : ${pct}% de l objectif atteint`, htmlContent })`
9. `logCronRun({ ..., jobName: 'revenue-alert', status: ..., details: { caMonth, target: user.ca_monthly_target, pct, emailSent: result.success, error: result.error } })`

**Erreurs TypeScript a anticiper:**
- `rdv.prospects` peut etre un objet ou un tableau selon le join Supabase -- utiliser `Array.isArray()` pour normaliser
- `SupabaseClient` de `@supabase/supabase-js` (pas de `@supabase/ssr`) pour cron-client -- les types sont differents
- `auth.admin` n'existe que sur le service_role client -- OK car createSupabaseCronClient utilise SERVICE_ROLE_KEY
  </action>
  <verify>
    <automated>cd "C:\Users\Ted\Documents\GitHub\TedScaleWithOuss" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - 4 fichiers route.ts crees sous src/app/api/cron/
    - Chaque route: verifyCronSecret en entree, createSupabaseCronClient (pas createSupabaseServiceClient), logCronRun en sortie
    - weekly-report: agrege CA semaine + CA mois + objectif + health alerts + relances + RDV -> email HTML dark/gold
    - client-health: RPC get_client_health_alerts -> email recap + SMS pour critiques
    - rdv-reminder: interactions J+1 -> WhatsApp (si configure) sinon SMS, sinon email fallback
    - revenue-alert: CA mois < target ET apres le 15 -> email alerte
    - npx tsc --noEmit passe sans erreur
    - Test manuel: curl -s -H "x-cron-secret: VOTRE_SECRET" http://localhost:3000/api/cron/weekly-report
      retourne { data: { status: 'ok', processed: 1, errors: [] }, error: null }
    - curl sans header retourne { data: null, error: 'Cron unauthorized' } avec status 401
    - Verifier table cron_logs dans Supabase apres chaque appel (au moins 1 ligne inseree)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task Scheduler (curl) -> /api/cron/* | Appel machine-to-machine sans session -- seul x-cron-secret authentifie |
| Route Handler -> Supabase (service_role) | Bypass RLS total -- acces a toutes les donnees de tous les users |
| Route Handler -> Brevo API | BREVO_API_KEY depuis process.env -- jamais exposee dans les logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05A-01 | Elevation of Privilege | /api/cron/* (4 routes) | mitigate | verifyCronSecret() verifie x-cron-secret -- 401 si invalide quand CRON_SECRET est defini dans .env.local |
| T-05A-02 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Dans .env.local uniquement, jamais prefixe NEXT_PUBLIC_, jamais dans les details JSONB de cron_logs |
| T-05A-03 | Information Disclosure | BREVO_API_KEY dans logs | mitigate | logCronRun() ne logue que status/counts/error_message -- jamais la valeur de la cle API |
| T-05A-04 | Denial of Service | Spam email si cron se declenche plusieurs fois | mitigate | revenue-alert: condition dayOfMonth >= 15 limite la frequence. client-health: skip si 0 alertes. |
| T-05A-05 | Spoofing | Routes ouvertes si CRON_SECRET absent | accept | Comportement dev intentionnel (localhost only). Documenter: CRON_SECRET obligatoire avant deploiement. |
</threat_model>

<verification>
Avec l'app Next.js demarree (`npm run dev`) et CRON_SECRET dans .env.local :

```powershell
# Definir le secret pour les tests
$env:CRON_SECRET = "votre_secret_de_test"

# AUTO-01 -- rapport hebdo
curl -s -H "x-cron-secret: $env:CRON_SECRET" http://localhost:3000/api/cron/weekly-report

# AUTO-02 -- alertes client health
curl -s -H "x-cron-secret: $env:CRON_SECRET" http://localhost:3000/api/cron/client-health

# AUTO-03 -- rappel RDV J-1
curl -s -H "x-cron-secret: $env:CRON_SECRET" http://localhost:3000/api/cron/rdv-reminder

# AUTO-04 -- alerte CA
curl -s -H "x-cron-secret: $env:CRON_SECRET" http://localhost:3000/api/cron/revenue-alert

# Test protection -- doit retourner 401
curl -s http://localhost:3000/api/cron/weekly-report
```

Chaque reponse doit etre `{ "data": { "status": "ok", "processed": N, "errors": [] }, "error": null }`.
Verifier la table cron_logs dans Supabase Table Editor apres chaque appel.
Appliquer la migration: `npx supabase db push` ou coller le SQL dans Supabase SQL Editor.
</verification>

<success_criteria>
- Les 4 routes retournent 200 avec { data: { status: 'ok', processed: N, errors: [] }, error: null }
- Un appel sans x-cron-secret retourne 401 avec { data: null, error: 'Cron unauthorized' }
- Table cron_logs dans Supabase contient 1 ligne par appel (job_name, status, details corrects)
- TypeScript compile sans erreur (npx tsc --noEmit)
- La migration 007_cron_logs.sql s'applique sans conflit sur le schema existant
- Les routes n'importent ni cookies() ni createSupabaseServiceClient (grep verifie l'absence)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/05-automatisations-cron/05-01-SUMMARY.md` avec :
- Liste des fichiers crees
- Decisions prises : cron-client sans cookies, WhatsApp avec fallback SMS, revenue-alert apres le 15
- Variables d'env requises : CRON_SECRET, BREVO_API_KEY, BREVO_SENDER_EMAIL, WHATSAPP_PHONE_NUMBER_ID (optionnel), WHATSAPP_ACCESS_TOKEN (optionnel)
- Commandes curl de test pour chaque route
</output>
