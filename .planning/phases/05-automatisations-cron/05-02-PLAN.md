---
phase: 05-automatisations-cron
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/app/(dashboard)/automatisations/page.tsx
  - scripts/setup-task-scheduler.ps1
autonomous: true
requirements:
  - AUTO-01
  - AUTO-02
  - AUTO-03
  - AUTO-04

must_haves:
  truths:
    - "La page /automatisations affiche le vrai journal d'execution depuis la table cron_logs (plus de EXEC_LOG statique)"
    - "La page affiche les 4 automatisations reelles (weekly-report, client-health, rdv-reminder, revenue-alert) avec leur statut last-run"
    - "Les KPIs (emails envoyes, SMS/WA envoyes) sont calcules depuis cron_logs.details JSONB"
    - "Le script setup-task-scheduler.ps1 cree les 4 taches Windows Task Scheduler en une seule commande"
    - "Les toggles de la page /automatisations sont purement visuels (pas de table BDD pour le on/off -- hors scope)"
  artifacts:
    - path: "src/app/(dashboard)/automatisations/page.tsx"
      provides: "Page /automatisations avec donnees reelles depuis /api/cron/logs"
      contains: "cron_logs"
    - path: "scripts/setup-task-scheduler.ps1"
      provides: "Script PowerShell de configuration Task Scheduler Windows"
      contains: "Register-ScheduledTask"
  key_links:
    - from: "src/app/(dashboard)/automatisations/page.tsx"
      to: "/api/cron/logs (route a creer dans ce plan)"
      via: "fetch('/api/cron/logs') au mount"
      pattern: "fetch.*cron.*logs"
    - from: "scripts/setup-task-scheduler.ps1"
      to: "http://localhost:3000/api/cron/* (4 routes)"
      via: "curl -H x-cron-secret dans les actions Task Scheduler"
      pattern: "Register-ScheduledTask"
---

<objective>
Brancher la page /automatisations sur les vraies donnees cron_logs et livrer le script PowerShell de setup Task Scheduler Windows.

Purpose: L'utilisateur voit l'historique reel des executions (plus de mocks), et peut configurer les 4 taches planifiees en une commande. Apres ce plan, la phase 5 est complete.

Output:
- /api/cron/logs : nouvelle route GET qui retourne les 50 derniers logs cron_logs pour l'utilisateur courant
- src/app/(dashboard)/automatisations/page.tsx : reecrit pour consommer /api/cron/logs
- scripts/setup-task-scheduler.ps1 : 4 taches Windows Task Scheduler
</objective>

<execution_context>
@C:\Users\Ted\Documents\GitHub\TedScaleWithOuss\.planning\workflows\execute-plan.md
</execution_context>

<context>
@C:\Users\Ted\Documents\GitHub\TedScaleWithOuss\.planning\ROADMAP.md
@C:\Users\Ted\Documents\GitHub\TedScaleWithOuss\.planning\phases\05-automatisations-cron\05-RESEARCH.md
@C:\Users\Ted\Documents\GitHub\TedScaleWithOuss\.planning\phases\05-automatisations-cron\05-01-SUMMARY.md

<interfaces>
<!-- Depuis le plan 05-01 -- types produits -->

Table cron_logs (depuis migration 007):
```typescript
interface CronLog {
  id: string
  user_id: string
  job_name: 'weekly-report' | 'client-health' | 'rdv-reminder' | 'revenue-alert'
  status: 'success' | 'error' | 'skipped'
  details: {
    // weekly-report: caWeek, caMonth, caObjective, alertsCount, emailSent
    // client-health: alertsCount, criticalCount, emailSent, smsSentCount
    // rdv-reminder: rdvCount, sentCount, errors
    // revenue-alert: caMonth, target, pct, emailSent
    [key: string]: unknown
  }
  executed_at: string // ISO timestamp
}
```

Depuis src/lib/api.ts:
```typescript
export function apiSuccess<T>(data: T, status?: number): NextResponse
export function apiError(message: string, status?: number): NextResponse
export function apiUnauthorized(): NextResponse
```

Depuis src/lib/supabase/server.ts (OK pour la route /api/cron/logs -- c'est une vraie route utilisateur authentifie):
```typescript
export async function createSupabaseServerClient(): Promise<SupabaseClient>
// Utiliser getUser() pour la route /api/cron/logs (c'est une route normale, pas un cron)
```

Depuis src/lib/theme.ts -- systeme de couleurs:
```typescript
// C.gold, C.cyan, C.green, C.textHi, C.textMid, C.textLo, C.textVlo
// C.surface1, C.surface2, C.surface3, C.bgMid, C.line, C.lineSoft
// C.ribbon (barre couleur header)
// JAMAIS de classes Tailwind ni de couleurs hexadecimales directes -- toujours C.*
```

Pattern page existante src/app/(dashboard)/automatisations/page.tsx:
- 'use client' en tete
- Composants Panel, PanelTitle, Toggle deja definis dans le fichier
- Header avec barre C.ribbon, titre Oswald uppercase, subtitle JetBrains Mono
- KPIs en grille 3 colonnes
- EXEC_LOG statique a remplacer par donnees reelles
- INITIAL_AUTOMATIONS statique a reordonner pour correspondre aux 4 jobs reels
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : Route /api/cron/logs + refonte page /automatisations avec donnees reelles</name>
  <files>
    src/app/api/cron/logs/route.ts
    src/app/(dashboard)/automatisations/page.tsx
  </files>
  <action>
**1. src/app/api/cron/logs/route.ts**

Route GET normale (utilisateur authentifie, pas un cron -- utilise createSupabaseServerClient) :

```typescript
import { NextRequest } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { apiSuccess, apiError, apiUnauthorized } from '@/lib/api'

export async function GET(_req: NextRequest) {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return apiUnauthorized()

  const { data, error } = await supabase
    .from('cron_logs')
    .select('id, job_name, status, details, executed_at')
    .eq('user_id', user.id)
    .order('executed_at', { ascending: false })
    .limit(50)

  if (error) return apiError(error.message)

  return apiSuccess(data ?? [])
}
```

**2. src/app/(dashboard)/automatisations/page.tsx**

Garder la structure visuelle existante (Panel, PanelTitle, Toggle, Header, grille KPIs). Remplacer uniquement les donnees mockees.

**Changements dans le composant AutomatisationsPage :**

a) Ajouter au state :
```typescript
const [logs, setLogs] = useState<CronLog[]>([])
const [loading, setLoading] = useState(true)
```

Avec le type interne :
```typescript
interface CronLog {
  id: string
  job_name: string
  status: 'success' | 'error' | 'skipped'
  details: Record<string, unknown>
  executed_at: string
}
```

b) Ajouter un useEffect pour fetch :
```typescript
useEffect(() => {
  fetch('/api/cron/logs')
    .then(r => r.json())
    .then(json => {
      if (json.data) setLogs(json.data)
    })
    .catch(() => {/* silencieux -- affiche [] */})
    .finally(() => setLoading(false))
}, [])
```

c) Remplacer INITIAL_AUTOMATIONS par une constante statique qui correspond aux 4 vrais jobs :
```typescript
const CRON_JOBS = [
  { id: 'weekly-report',  icon: '📊', iconBg: '#0d1f0f', name: 'Rapport hebdomadaire', desc: 'Email KPIs chaque lundi 8h via Brevo', active: true },
  { id: 'client-health',  icon: '🔔', iconBg: '#1f0d0d', name: 'Alertes Client Health', desc: 'Email + SMS quotidien si inactivite', active: true },
  { id: 'rdv-reminder',   icon: '💬', iconBg: '#0a1a0a', name: 'Rappel RDV J-1', desc: 'SMS / WhatsApp chaque soir 18h', active: true },
  { id: 'revenue-alert',  icon: '⚠',  iconBg: '#1a1400', name: 'Alerte CA sous seuil', desc: 'Email si CA mensuel < objectif', active: true },
]
```

Garder le state `automations` initialise depuis CRON_JOBS (pour que les toggles fonctionnent visuellement -- ils ne persistont pas en BDD, comportement ok pour cette phase).

d) Calculer les KPIs depuis les logs :
```typescript
const totalEmailsSent = logs.reduce((sum, log) => {
  const d = log.details
  return sum + (d.emailSent ? 1 : 0)
}, 0)

const totalSmsSent = logs.reduce((sum, log) => {
  const d = log.details
  const sms = typeof d.smsSentCount === 'number' ? d.smsSentCount : 0
  const sent = typeof d.sentCount === 'number' && log.job_name === 'rdv-reminder' ? d.sentCount : 0
  return sum + sms + sent
}, 0)
```

Utiliser ces valeurs dans la grille KPIs (remplacer '148' et '37' statiques).

e) Remplacer EXEC_LOG par les vraies donnees :

Dans la section "Journal d'execution", remplacer le mapping de `EXEC_LOG` par un mapping de `logs` :
```typescript
{loading ? (
  <div style={{ fontFamily: 'JetBrains Mono,monospace', fontSize: 10, color: C.textLo, padding: '12px 10px' }}>
    Chargement...
  </div>
) : logs.length === 0 ? (
  <div style={{ fontFamily: 'JetBrains Mono,monospace', fontSize: 10, color: C.textLo, padding: '12px 10px' }}>
    Aucune execution enregistree -- lancer une premiere tache via curl ou Task Scheduler
  </div>
) : logs.map((log, i) => {
  const jobLabels: Record<string, string> = {
    'weekly-report': 'Rapport hebdo',
    'client-health': 'Alerte client health',
    'rdv-reminder': 'Rappel RDV J-1',
    'revenue-alert': 'Alerte CA',
  }
  const actionSummary = buildActionSummary(log)  // voir helper ci-dessous
  const ts = new Date(log.executed_at).toLocaleString('fr-FR', { day: '2-digit', month: '2-digit', hour: '2-digit', minute: '2-digit' })
  const isSuccess = log.status === 'success'
  const isSkipped = log.status === 'skipped'

  return (
    <div key={log.id} style={{
      display: 'grid', gridTemplateColumns: '140px 120px 80px 1fr', gap: 12,
      padding: '8px 10px', borderRadius: 6,
      background: i % 2 === 0 ? C.surface2 : 'transparent',
      borderLeft: `2px solid ${isSuccess ? C.green : isSkipped ? C.textLo : C.cyan}`,
    }}>
      <div style={{ fontFamily: 'JetBrains Mono,monospace', fontSize: 10, fontWeight: 500, color: C.textHi, whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>
        {jobLabels[log.job_name] ?? log.job_name}
      </div>
      <div style={{ fontFamily: 'JetBrains Mono,monospace', fontSize: 10, color: C.textLo }}>{ts}</div>
      <div>
        <span style={{
          fontFamily: 'JetBrains Mono,monospace', fontSize: 9, fontWeight: 700,
          color: isSuccess ? C.green : isSkipped ? C.textLo : C.cyan,
          background: isSuccess ? `${C.green}22` : isSkipped ? `${C.textLo}22` : `${C.cyan}22`,
          padding: '2px 8px', borderRadius: 8, textTransform: 'uppercase',
        }}>
          {isSuccess ? '✓ OK' : isSkipped ? '— SKIP' : '✗ ERR'}
        </span>
      </div>
      <div style={{ fontFamily: 'JetBrains Mono,monospace', fontSize: 10, color: C.textMid, whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>
        {actionSummary}
      </div>
    </div>
  )
})}
```

Helper `buildActionSummary(log: CronLog): string` (definir avant le composant) :
```typescript
function buildActionSummary(log: CronLog): string {
  const d = log.details
  switch (log.job_name) {
    case 'weekly-report':
      return d.emailSent
        ? `Email envoye -- CA ${typeof d.caMonth === 'number' ? d.caMonth.toLocaleString('fr-FR') : '?'} EUR, ${d.alertsCount ?? 0} alerte(s)`
        : d.error ? `Erreur: ${d.error}` : 'Email non envoye'
    case 'client-health':
      if (log.status === 'skipped') return d.reason as string ?? 'Aucune alerte'
      return `${d.alertsCount ?? 0} alerte(s) -- Email ${d.emailSent ? 'envoye' : 'echec'}${typeof d.smsSentCount === 'number' && d.smsSentCount > 0 ? `, ${d.smsSentCount} SMS` : ''}`
    case 'rdv-reminder':
      if (log.status === 'skipped') return d.reason as string ?? 'Aucun RDV demain'
      return `${d.rdvCount ?? 0} RDV -- ${d.sentCount ?? 0} message(s) envoye(s)`
    case 'revenue-alert':
      if (log.status === 'skipped') return d.reason as string ?? 'CA OK ou trop tot'
      return `CA ${typeof d.caMonth === 'number' ? d.caMonth.toLocaleString('fr-FR') : '?'} EUR / ${typeof d.target === 'number' ? d.target.toLocaleString('fr-FR') : '?'} EUR (${d.pct ?? 0}%)`
    default:
      return log.status
  }
}
```

f) Mettre a jour le titre du panneau journal :
`"Journal d'execution -- 50 derniers runs"`

g) Le composant Panel, PanelTitle et Toggle existants dans le fichier sont a conserver tels quels -- ne pas les modifier.
  </action>
  <verify>
    <automated>cd "C:\Users\Ted\Documents\GitHub\TedScaleWithOuss" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/app/api/cron/logs/route.ts cree : GET retourne cron_logs triees DESC pour l'utilisateur courant, limit 50
    - automatisations/page.tsx : useEffect fetch /api/cron/logs, logs affiches dans le journal, EXEC_LOG statique supprime
    - KPIs emails et SMS calcules depuis logs.details (pas hardcodes)
    - INITIAL_AUTOMATIONS remplace par CRON_JOBS avec les 4 vrais job IDs
    - Etat loading affiche "Chargement..." pendant le fetch, etat vide affiche message instructif
    - Les composants Panel/PanelTitle/Toggle et le style dark/gold sont preserves
    - npx tsc --noEmit passe sans erreur
    - En dev avec l'app ouverte: naviguer sur /automatisations montre le journal reel (ou "Aucune execution" si vide)
  </done>
</task>

<task type="auto">
  <name>Tache 2 : Script PowerShell setup-task-scheduler.ps1 pour les 4 taches Windows</name>
  <files>
    scripts/setup-task-scheduler.ps1
  </files>
  <action>
Creer le repertoire `scripts/` a la racine du projet si absent.

Creer `scripts/setup-task-scheduler.ps1` :

```powershell
# setup-task-scheduler.ps1
# Configure les 4 taches planifiees Windows pour Ted Scale With Ouss
#
# Usage:
#   1. Definir CRON_SECRET dans .env.local : CRON_SECRET=votre_secret_ici
#   2. Copier votre secret ci-dessous dans $CronSecret
#   3. Executer en mode Admin: .\scripts\setup-task-scheduler.ps1
#
# Prerequis: App Next.js demarree sur http://localhost:3000

param(
  [string]$CronSecret = $env:CRON_SECRET,
  [string]$AppUrl = "http://localhost:3000"
)

if (-not $CronSecret) {
  Write-Error "CRON_SECRET non defini. Definissez-le dans .env.local ou passez -CronSecret 'votre_secret'"
  exit 1
}

$curlPath = "curl.exe"
Write-Host "Configuration des taches Task Scheduler pour Ted Scale With Ouss..." -ForegroundColor Cyan

# ============================================================
# AUTO-01 : Rapport hebdomadaire -- chaque lundi a 8h
# ============================================================
$action1 = New-ScheduledTaskAction `
  -Execute $curlPath `
  -Argument "-s -X GET -H `"x-cron-secret: $CronSecret`" $AppUrl/api/cron/weekly-report"

$trigger1 = New-ScheduledTaskTrigger -Weekly -DaysOfWeek Monday -At "08:00"

$settings1 = New-ScheduledTaskSettingsSet `
  -ExecutionTimeLimit (New-TimeSpan -Minutes 5) `
  -StartWhenAvailable

Register-ScheduledTask `
  -TaskName "TedCGP-WeeklyReport" `
  -Action $action1 `
  -Trigger $trigger1 `
  -Settings $settings1 `
  -Description "Ted Scale -- Rapport hebdomadaire email (AUTO-01)" `
  -Force

Write-Host "[OK] TedCGP-WeeklyReport -- Lundi 08h00" -ForegroundColor Green

# ============================================================
# AUTO-02 : Alertes Client Health -- chaque jour a 7h
# ============================================================
$action2 = New-ScheduledTaskAction `
  -Execute $curlPath `
  -Argument "-s -X GET -H `"x-cron-secret: $CronSecret`" $AppUrl/api/cron/client-health"

$trigger2 = New-ScheduledTaskTrigger -Daily -At "07:00"

$settings2 = New-ScheduledTaskSettingsSet `
  -ExecutionTimeLimit (New-TimeSpan -Minutes 5) `
  -StartWhenAvailable

Register-ScheduledTask `
  -TaskName "TedCGP-ClientHealth" `
  -Action $action2 `
  -Trigger $trigger2 `
  -Settings $settings2 `
  -Description "Ted Scale -- Alertes client inactif (AUTO-02)" `
  -Force

Write-Host "[OK] TedCGP-ClientHealth -- Tous les jours 07h00" -ForegroundColor Green

# ============================================================
# AUTO-03 : Rappel RDV J-1 -- chaque jour a 18h
# ============================================================
$action3 = New-ScheduledTaskAction `
  -Execute $curlPath `
  -Argument "-s -X GET -H `"x-cron-secret: $CronSecret`" $AppUrl/api/cron/rdv-reminder"

$trigger3 = New-ScheduledTaskTrigger -Daily -At "18:00"

$settings3 = New-ScheduledTaskSettingsSet `
  -ExecutionTimeLimit (New-TimeSpan -Minutes 5) `
  -StartWhenAvailable

Register-ScheduledTask `
  -TaskName "TedCGP-RdvReminder" `
  -Action $action3 `
  -Trigger $trigger3 `
  -Settings $settings3 `
  -Description "Ted Scale -- Rappel RDV SMS/WhatsApp J-1 (AUTO-03)" `
  -Force

Write-Host "[OK] TedCGP-RdvReminder -- Tous les jours 18h00" -ForegroundColor Green

# ============================================================
# AUTO-04 : Alerte CA < seuil -- chaque jour a 9h
# ============================================================
$action4 = New-ScheduledTaskAction `
  -Execute $curlPath `
  -Argument "-s -X GET -H `"x-cron-secret: $CronSecret`" $AppUrl/api/cron/revenue-alert"

$trigger4 = New-ScheduledTaskTrigger -Daily -At "09:00"

$settings4 = New-ScheduledTaskSettingsSet `
  -ExecutionTimeLimit (New-TimeSpan -Minutes 5) `
  -StartWhenAvailable

Register-ScheduledTask `
  -TaskName "TedCGP-RevenueAlert" `
  -Action $action4 `
  -Trigger $trigger4 `
  -Settings $settings4 `
  -Description "Ted Scale -- Alerte CA mensuel sous objectif (AUTO-04)" `
  -Force

Write-Host "[OK] TedCGP-RevenueAlert -- Tous les jours 09h00" -ForegroundColor Green

# ============================================================
# Recapitulatif
# ============================================================
Write-Host ""
Write-Host "4 taches configurees avec succes :" -ForegroundColor Cyan
Write-Host "  TedCGP-WeeklyReport   -- Lundi 08:00"
Write-Host "  TedCGP-ClientHealth   -- Quotidien 07:00"
Write-Host "  TedCGP-RdvReminder    -- Quotidien 18:00"
Write-Host "  TedCGP-RevenueAlert   -- Quotidien 09:00"
Write-Host ""
Write-Host "IMPORTANT: L'app Next.js doit etre demarree (npm run dev) pour que les taches fonctionnent." -ForegroundColor Yellow
Write-Host "Verifiez les executions dans : /automatisations (journal d'execution)" -ForegroundColor Yellow
Write-Host ""
Write-Host "Pour supprimer toutes les taches :" -ForegroundColor Gray
Write-Host "  'TedCGP-WeeklyReport','TedCGP-ClientHealth','TedCGP-RdvReminder','TedCGP-RevenueAlert' | ForEach-Object { Unregister-ScheduledTask -TaskName `$_ -Confirm:`$false }" -ForegroundColor Gray
```
  </action>
  <verify>
    <automated>cd "C:\Users\Ted\Documents\GitHub\TedScaleWithOuss" && Get-Content scripts/setup-task-scheduler.ps1 | Select-String "Register-ScheduledTask" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
    - scripts/setup-task-scheduler.ps1 existe avec 4 appels Register-ScheduledTask (un par automatisation)
    - Le script lit CRON_SECRET depuis $env:CRON_SECRET ou le parametre -CronSecret
    - Planning: lundi 8h (rapport), quotidien 7h (health), quotidien 18h (RDV), quotidien 9h (CA)
    - Le script affiche un recapitulatif colore apres creation
    - Test: `Get-ScheduledTask -TaskName "TedCGP-*"` liste les 4 taches apres execution du script en mode Admin
    - Les noms des taches correspondent a ce qui est documente dans la RESEARCH.md
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /api/cron/logs | Route normale -- getUser() verifie la session Supabase |
| setup-task-scheduler.ps1 -> CRON_SECRET | Secret passe en parametre ou variable d'env -- ne pas le committer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05B-01 | Information Disclosure | /api/cron/logs | mitigate | getUser() + .eq('user_id', user.id) -- chaque user voit uniquement ses propres logs (RLS + filtre applicatif double protection) |
| T-05B-02 | Information Disclosure | CRON_SECRET dans setup-task-scheduler.ps1 | mitigate | Le script lit $env:CRON_SECRET ou -CronSecret param -- ne pas hardcoder dans le fichier. Le fichier lui-meme ne contient pas le secret. |
| T-05B-03 | Tampering | Toggles automatisations sans persistance | accept | Les toggles sont purement visuels (state local React). Risque minimal : la persistance des toggles est hors scope Phase 5. |
</threat_model>

<verification>
```powershell
# 1. Verifier la route /api/cron/logs (app demarree, utilisateur connecte)
curl -s http://localhost:3000/api/cron/logs
# Attendu: { data: [...], error: null } -- tableau de cron_logs (vide si aucun cron execute)

# 2. Verifier la page /automatisations dans le navigateur
# Naviguer sur http://localhost:3000/automatisations
# Le journal doit afficher "Aucune execution enregistree..." si vide
# Ou les vrais logs apres avoir execute au moins un cron via curl

# 3. Tester le script Task Scheduler (mode Admin PowerShell)
# Definir le secret de test
$env:CRON_SECRET = "test_secret_local"
# Executer le script
.\scripts\setup-task-scheduler.ps1
# Verifier la creation des 4 taches
Get-ScheduledTask -TaskName "TedCGP-*" | Select-Object TaskName, State

# 4. Cycle complet de test:
# a. Executer un cron manuellement via curl
curl -s -H "x-cron-secret: test_secret_local" http://localhost:3000/api/cron/client-health
# b. Recharger /automatisations -- le log doit apparaitre dans le journal
```
</verification>

<success_criteria>
- GET /api/cron/logs retourne 200 avec un tableau de cron_logs pour l'utilisateur connecte (protege par getUser())
- La page /automatisations affiche les vraies donnees de cron_logs (pas EXEC_LOG statique)
- Les KPIs emails/SMS sont calcules depuis logs.details (valeurs dynamiques)
- Les 4 CRON_JOBS correspondent exactement aux job_name des 4 routes cron
- setup-task-scheduler.ps1 cree 4 taches Task Scheduler sans erreur quand execute en mode Admin
- npx tsc --noEmit passe sans erreur sur tous les fichiers modifies
</success_criteria>

<output>
Apres completion, creer `.planning/phases/05-automatisations-cron/05-02-SUMMARY.md` avec :
- Fichiers crees/modifies
- Comment executer le script Task Scheduler (mode Admin requis)
- Comment verifier que les taches sont bien enregistrees (Get-ScheduledTask)
- Lien vers la page /automatisations pour voir le journal reel
- Note: Phase 5 complete -- toutes les automatisations fonctionnelles
</output>
