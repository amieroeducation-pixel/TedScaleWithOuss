---
phase: 04-achievements-artefacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/006_achievements.sql
  - src/app/api/achievements/check/route.ts
  - src/app/api/achievements/route.ts
  - src/types/achievements.ts
  - src/components/achievements/AchievementsProvider.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true
requirements:
  - ACH-01
  - ACH-02
  - ACH-03
  - ACH-05

must_haves:
  truths:
    - "La table achievements existe en BDD avec UNIQUE(user_id, achievement_key)"
    - "POST /api/achievements/check insère les nouveaux achievements et retourne seulement ceux fraichement créés"
    - "Un doublon (même user_id + achievement_key) ne crée pas de second enregistrement"
    - "AchievementsProvider se monte dans le layout, appelle /check au montage (une seule fois), affiche un toast Sonner et dispatch celebrate:all"
    - "L'animation fire/confetti se déclenche côté client via window.dispatchEvent uniquement si nouvel achievement"
    - "celebrations.js est chargé via <Script strategy='lazyOnload' /> dans le layout"
  artifacts:
    - path: "supabase/migrations/006_achievements.sql"
      provides: "Table achievements avec UNIQUE(user_id, achievement_key), RLS, index"
    - path: "src/types/achievements.ts"
      provides: "Type Achievement exporté, constantes CLIENT_MILESTONES et MONTH_LABELS"
    - path: "src/app/api/achievements/check/route.ts"
      provides: "POST route — détection + upsert ignoreDuplicates + retour newAchievements"
    - path: "src/app/api/achievements/route.ts"
      provides: "GET route — liste achievements de l'utilisateur triés par achieved_at DESC"
    - path: "src/components/achievements/AchievementsProvider.tsx"
      provides: "Client Component — fetch check + toast Sonner + dispatchEvent celebrate:all"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Layout modifié — Script celebrations.js + AchievementsProvider"
  key_links:
    - from: "src/components/achievements/AchievementsProvider.tsx"
      to: "/api/achievements/check"
      via: "fetch POST dans useEffect"
      pattern: "fetch.*achievements/check"
    - from: "src/app/api/achievements/check/route.ts"
      to: "supabase achievements table"
      via: "upsert ignoreDuplicates"
      pattern: "upsert.*ignoreDuplicates"
    - from: "src/components/achievements/AchievementsProvider.tsx"
      to: "public/celebrations.js"
      via: "window.dispatchEvent(new CustomEvent('celebrate:all'))"
      pattern: "celebrate:all"
---

<objective>
Poser l'infrastructure complète des achievements : migration BDD, routes API de détection/persistance, composant Provider invisible qui déclenche badge + célébration au premier chargement du dashboard.

Purpose: ACH-01 (badge CA), ACH-02 (animation feu/confetti), ACH-03 (badge clients), ACH-05 (anti-redéclenchement). L'utilisateur voit la célébration exactement une fois par achievement, même après rechargement — la contrainte UNIQUE en BDD est la source de vérité.

Output: Table achievements, 2 routes API, type partagé, AchievementsProvider dans le layout.
</objective>

<execution_context>
@C:/Users/Ted/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ted/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Pattern migration existant : voir supabase/migrations/005_sequences.sql -->
<!-- API pattern : voir src/app/api/revenue/stats/route.ts (getUser, apiSuccess, apiUnauthorized) -->
<!-- Layout actuel : 'use client', Toaster déjà présent ligne 434, NAV_SECTIONS inclut '/champions' -->

<interfaces>
<!-- Types et contrats à utiliser directement — pas besoin d'explorer la codebase -->

Depuis src/lib/theme.ts:
```typescript
export const C: Record<string, string> = {
  bgDeep: '#0a0e22', bgMid: '#14193d',
  surface1: '#11163a', surface2: '#1a2150', surface3: '#252e68',
  line: '#3a4690', lineSoft: '#1a2150',
  textHi: '#ffffff', text: '#d8e1ff', textMid: '#8ea0d9',
  textLo: '#5a6ba8', textVlo: '#3a4885',
  gold: '#e8c878', warn: '#d8884a', green: '#4ade80',
  ribbon: 'linear-gradient(90deg,#c84048 0%,#ff6470 25%,#f5e8c8 55%,#7a92e8 80%,#5c70b8 100%)',
}
```

Depuis src/app/api/revenue/stats/route.ts (pattern auth + supabase):
```typescript
import { NextRequest } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { apiSuccess, apiError, apiUnauthorized } from '@/lib/api'

const { data: { user } } = await supabase.auth.getUser()
if (!user) return apiUnauthorized()
```

Depuis src/app/api/revenue/stats/route.ts (données disponibles):
```typescript
// v_monthly_revenue : month_num, revenue, objective, pct_of_objective
// user_settings : ca_monthly_target
// clients table : count avec .eq('user_id', user.id)
// objectiveMonth (number) — objectif mensuel en euros
// caCurrentMonth (number) — CA réalisé ce mois
// clientCount (number) — nb clients actifs
```

Layout src/app/(dashboard)/layout.tsx:
```typescript
'use client' // ligne 1 — Client Component
// Toaster déjà présent en fin de return (ligne 434)
// <Toaster theme="dark" position="bottom-right" richColors />
// Ajouter AVANT le Toaster : <AchievementsProvider />
// Ajouter APRES le Toaster : <Script src="/celebrations.js" strategy="lazyOnload" />
// Import à ajouter en haut : import Script from 'next/script'
// Import à ajouter en haut : import { AchievementsProvider } from '@/components/achievements/AchievementsProvider'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : Migration 006 + types Achievement partagés</name>
  <files>supabase/migrations/006_achievements.sql, src/types/achievements.ts</files>
  <action>
Créer la migration SQL et le fichier de types TypeScript partagé.

**006_achievements.sql** — Suivre exactement le même style que 005_sequences.sql (commentaires, casse minuscule, sections séparées) :

```sql
-- Migration 006 : Achievements & Artefacts (Phase 4)

-- ============================================================
-- TABLE achievements
-- ============================================================

create table achievements (
  id               uuid primary key default gen_random_uuid(),
  user_id          uuid references auth.users(id) on delete cascade not null,
  achievement_key  text not null,
  achievement_type text not null check (achievement_type in ('ca_monthly', 'clients_milestone')),
  label            text not null,
  value            numeric,
  target           numeric,
  achieved_at      timestamptz not null default now(),
  created_at       timestamptz not null default now(),
  unique (user_id, achievement_key)
);

create index idx_achievements_user_id on achievements(user_id);
create index idx_achievements_achieved_at on achievements(achieved_at desc);

-- ============================================================
-- RLS : achievements
-- ============================================================

alter table achievements enable row level security;
create policy "Users can view their own achievements" on achievements
  for select using (auth.uid() = user_id);
create policy "Users can insert their own achievements" on achievements
  for insert with check (auth.uid() = user_id);
```

**src/types/achievements.ts** — Types partagés consommés par les routes API et le Provider :

```typescript
export type AchievementType = 'ca_monthly' | 'clients_milestone'

export interface Achievement {
  id: string
  user_id: string
  achievement_key: string
  achievement_type: AchievementType
  label: string
  value: number | null
  target: number | null
  achieved_at: string
  created_at: string
}

export const CLIENT_MILESTONES = [10, 25, 50] as const

export const MONTH_LABELS_FR = [
  'Janvier', 'Février', 'Mars', 'Avril', 'Mai', 'Juin',
  'Juillet', 'Août', 'Septembre', 'Octobre', 'Novembre', 'Décembre',
] as const
```

Appliquer la migration via Supabase CLI : `npx supabase db push` (si Supabase CLI est disponible) OU laisser un commentaire `-- À appliquer : npx supabase migration up` si la migration est créée pour application manuelle ultérieure. Ne pas bloquer sur cette commande.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const sql=fs.readFileSync('supabase/migrations/006_achievements.sql','utf8'); const ts=fs.readFileSync('src/types/achievements.ts','utf8'); console.assert(sql.includes('unique (user_id, achievement_key)'), 'UNIQUE manquant'); console.assert(sql.includes('row level security'), 'RLS manquant'); console.assert(ts.includes('Achievement'), 'Type manquant'); console.assert(ts.includes('CLIENT_MILESTONES'), 'Constantes manquantes'); console.log('OK')"</automated>
  </verify>
  <done>006_achievements.sql contient la table, la contrainte UNIQUE, RLS. src/types/achievements.ts exporte Achievement, CLIENT_MILESTONES, MONTH_LABELS_FR.</done>
</task>

<task type="auto">
  <name>Tache 2 : Routes API /api/achievements/check (POST) et /api/achievements (GET)</name>
  <files>src/app/api/achievements/check/route.ts, src/app/api/achievements/route.ts</files>
  <action>
Créer les deux routes API. Pattern identique à /api/revenue/stats/route.ts : import NextRequest, createSupabaseServerClient, apiSuccess, apiUnauthorized.

**src/app/api/achievements/check/route.ts** — Route POST, detects et persiste les nouveaux achievements :

```typescript
import { NextRequest } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { apiSuccess, apiUnauthorized } from '@/lib/api'
import { Achievement, CLIENT_MILESTONES, MONTH_LABELS_FR } from '@/types/achievements'

export async function POST(_request: NextRequest) {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return apiUnauthorized()

  const now = new Date()
  const year = now.getFullYear()
  const month = now.getMonth() + 1

  // Récupérer CA du mois courant depuis v_monthly_revenue
  const { data: revenueRow } = await supabase
    .from('v_monthly_revenue')
    .select('revenue')
    .eq('user_id', user.id)
    .eq('year', year)
    .eq('month_num', month)
    .single()

  // Récupérer objectif mensuel depuis revenue_objectives (fallback user_settings)
  const { data: objRow } = await supabase
    .from('revenue_objectives')
    .select('amount')
    .eq('user_id', user.id)
    .eq('year', year)
    .eq('month', month)
    .is('product_type', null)
    .single()

  const { data: settingsRow } = await supabase
    .from('user_settings')
    .select('ca_monthly_target')
    .eq('id', user.id)
    .single()

  // Récupérer count clients
  const { count: clientCount } = await supabase
    .from('clients')
    .select('*', { count: 'exact', head: true })
    .eq('user_id', user.id)

  const caCurrentMonth = Number(revenueRow?.revenue ?? 0)
  const caTarget = Number(objRow?.amount ?? settingsRow?.ca_monthly_target ?? 15000)
  const clients = clientCount ?? 0

  const newAchievements: Achievement[] = []

  // Vérifier objectif CA mensuel (ACH-01, ACH-02)
  if (caCurrentMonth >= caTarget && caTarget > 0) {
    const key = `ca_monthly_${year}_${String(month).padStart(2, '0')}`
    const label = `Objectif CA ${MONTH_LABELS_FR[month - 1]} ${year} atteint`
    const { data } = await supabase
      .from('achievements')
      .upsert(
        { user_id: user.id, achievement_key: key, achievement_type: 'ca_monthly', label, value: caCurrentMonth, target: caTarget },
        { onConflict: 'user_id,achievement_key', ignoreDuplicates: true }
      )
      .select()
      .single()
    // data === null si doublon (ignoreDuplicates retourne null quand pas d'insert)
    if (data) newAchievements.push(data as Achievement)
  }

  // Vérifier seuils clients actifs (ACH-03)
  for (const milestone of CLIENT_MILESTONES) {
    if (clients >= milestone) {
      const key = `clients_${milestone}`
      const label = `${milestone} clients actifs`
      const { data } = await supabase
        .from('achievements')
        .upsert(
          { user_id: user.id, achievement_key: key, achievement_type: 'clients_milestone', label, value: clients, target: milestone },
          { onConflict: 'user_id,achievement_key', ignoreDuplicates: true }
        )
        .select()
        .single()
      if (data) newAchievements.push(data as Achievement)
    }
  }

  return apiSuccess({ newAchievements })
}
```

**src/app/api/achievements/route.ts** — Route GET, retourne tous les achievements de l'utilisateur pour la timeline (ACH-04) :

```typescript
import { NextRequest } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { apiSuccess, apiUnauthorized } from '@/lib/api'

export async function GET(_request: NextRequest) {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return apiUnauthorized()

  const { data: achievements, error } = await supabase
    .from('achievements')
    .select('*')
    .eq('user_id', user.id)
    .order('achieved_at', { ascending: false })

  if (error) {
    // Table peut ne pas exister si migration pas encore appliquée
    return apiSuccess({ achievements: [] })
  }

  return apiSuccess({ achievements: achievements ?? [] })
}
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const check=fs.readFileSync('src/app/api/achievements/check/route.ts','utf8'); const get=fs.readFileSync('src/app/api/achievements/route.ts','utf8'); console.assert(check.includes('ignoreDuplicates: true'), 'ignoreDuplicates manquant'); console.assert(check.includes('celebrate') || check.includes('newAchievements'), 'logique achievement manquante'); console.assert(check.includes('getUser'), 'auth manquante'); console.assert(get.includes('achieved_at'), 'ordre timeline manquant'); console.log('OK')"</automated>
  </verify>
  <done>POST /api/achievements/check retourne { data: { newAchievements: Achievement[] } } — vide si tout déjà en BDD. GET /api/achievements retourne { data: { achievements: Achievement[] } } triés par achieved_at DESC.</done>
</task>

<task type="auto">
  <name>Tache 3 : AchievementsProvider + intégration layout</name>
  <files>src/components/achievements/AchievementsProvider.tsx, src/app/(dashboard)/layout.tsx</files>
  <action>
Créer le composant Provider invisible puis modifier le layout pour l'intégrer avec le Script celebrations.js.

**src/components/achievements/AchievementsProvider.tsx** :

```typescript
'use client'

import { useEffect, useRef } from 'react'
import { toast } from 'sonner'
import type { Achievement } from '@/types/achievements'

export function AchievementsProvider() {
  const checked = useRef(false)

  useEffect(() => {
    // Une seule vérification par session — useRef persiste entre re-renders
    if (checked.current) return
    checked.current = true

    fetch('/api/achievements/check', { method: 'POST' })
      .then(r => r.json())
      .then(({ data }) => {
        const newAchievements: Achievement[] = data?.newAchievements ?? []
        if (!newAchievements.length) return

        // Toast Sonner pour chaque badge débloqué (ACH-01, ACH-03)
        newAchievements.forEach((a) => {
          toast.success(`Badge debloque : ${a.label}`, {
            duration: 6000,
            description: a.achievement_type === 'ca_monthly'
              ? `CA : ${a.value?.toLocaleString('fr-FR')} € / objectif ${a.target?.toLocaleString('fr-FR')} €`
              : `${a.value} clients dans votre portefeuille`,
          })
        })

        // Délai court pour laisser celebrations.js s'initialiser (strategy lazyOnload)
        // ACH-02 : dispatch l'événement celebrate:all
        setTimeout(() => {
          if (typeof window !== 'undefined') {
            window.dispatchEvent(new CustomEvent('celebrate:all', {
              detail: { text: 'OBJECTIF !' }
            }))
          }
        }, 800)
      })
      .catch(() => {
        // Silencieux — ne pas bloquer le dashboard si la table n'existe pas encore
      })
  }, [])

  return null
}
```

**Modification src/app/(dashboard)/layout.tsx** — Ajouter en haut du fichier (après les imports existants) :

```typescript
import Script from 'next/script'
import { AchievementsProvider } from '@/components/achievements/AchievementsProvider'
```

Dans le JSX, juste avant la fermeture `</>` finale (après `<Toaster ... />`), ajouter :

```typescript
      <AchievementsProvider />
      <Script src="/celebrations.js" strategy="lazyOnload" />
```

Le layout existant se termine par :
```typescript
      <Toaster theme="dark" position="bottom-right" richColors />
    </>   ← ajouter AVANT cette ligne
```

Ne pas modifier le reste du layout — ajouter uniquement ces deux éléments à la fin du fragment JSX.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const provider=fs.readFileSync('src/components/achievements/AchievementsProvider.tsx','utf8'); const layout=fs.readFileSync('src/app/(dashboard)/layout.tsx','utf8'); console.assert(provider.includes(\"'use client'\"), 'use client manquant'); console.assert(provider.includes('checked.current'), 'ref anti-double manquant'); console.assert(provider.includes('celebrate:all'), 'dispatch event manquant'); console.assert(provider.includes('toast.success'), 'toast manquant'); console.assert(layout.includes('AchievementsProvider'), 'Provider non integre au layout'); console.assert(layout.includes('celebrations.js'), 'Script celebrations manquant'); console.assert(layout.includes('lazyOnload'), 'strategy lazyOnload manquante'); console.log('OK')"</automated>
  </verify>
  <done>AchievementsProvider monté dans le layout — au chargement du dashboard, si un nouvel achievement est détecté : toast Sonner + animation celebrate:all. Si déjà en BDD : aucun effet visuel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> POST /api/achievements/check | Route invoquée par le Provider côté client — doit valider l'authentification |
| API -> Supabase achievements | INSERT avec user_id forcé côté serveur, pas côté client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | POST /api/achievements/check | mitigate | `getUser()` serveur obligatoire — user_id vient du JWT, jamais du body |
| T-04-02 | Tampering | Upsert achievements | mitigate | RLS `auth.uid() = user_id` — un utilisateur ne peut écrire que ses propres achievements |
| T-04-03 | Repudiation | achievements table | accept | Usage solo CGP — audit trail non requis pour cette phase |
| T-04-04 | Information Disclosure | GET /api/achievements | mitigate | RLS Supabase filtre par user_id — pas de fuite inter-utilisateurs |
| T-04-05 | Denial of Service | celebrations.js dispatch | accept | Événement local browser, aucun impact serveur |
| T-04-06 | Elevation of Privilege | upsert ignoreDuplicates | mitigate | user_id toujours issu de `supabase.auth.getUser()` côté serveur — pas de param client |
</threat_model>

<verification>
Vérification fonctionnelle complète (en local) :

1. Appliquer la migration : `npx supabase db push` ou `npx supabase migration up`
2. Ouvrir le dashboard dans Chrome — vérifier dans l'onglet Network que POST /api/achievements/check est appelé une seule fois au chargement
3. Si conditions remplies (CA >= objectif OU nb clients >= 10/25/50) : toast Sonner visible + animation confetti
4. Recharger la page : AUCUN toast, AUCUNE animation (achievement déjà en BDD)
5. Vérifier dans Supabase Studio (ou via `npx supabase db inspect`) que la table achievements contient les lignes avec les bons achievement_key
</verification>

<success_criteria>
- Table achievements créée avec UNIQUE(user_id, achievement_key) et RLS actives
- POST /api/achievements/check retourne newAchievements: [] pour un utilisateur dont les achievements sont déjà persistés
- AchievementsProvider monté dans le layout sans erreur TypeScript ni erreur "window is not defined"
- Toast + animation déclenchés exactement une fois par achievement — pas de re-déclenchement au rechargement
- Zéro erreur console côté serveur sur les upserts (ignoreDuplicates gère silencieusement les doublons)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/04-achievements-artefacts/04-01-SUMMARY.md` avec :
- Fichiers créés/modifiés
- Clés d'achievement utilisées (ca_monthly_YYYY_MM, clients_10/25/50)
- Confirmation que l'upsert ignoreDuplicates fonctionne correctement (ou erreur observée)
- Pattern exact utilisé pour le dispatch celebrate:all
</output>
