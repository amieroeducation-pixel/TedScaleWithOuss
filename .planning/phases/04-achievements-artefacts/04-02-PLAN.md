---
phase: 04-achievements-artefacts
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/(dashboard)/achievements/page.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true
requirements:
  - ACH-04

must_haves:
  truths:
    - "L'utilisateur peut naviguer vers /achievements depuis la sidebar (lien '🏆 Champions' ou nouveau lien)"
    - "La page /achievements affiche la liste de tous les achievements débloqués, triés par date décroissante"
    - "Chaque achievement affiche son label, sa date (format lisible fr-FR), et son type visuellement distinct"
    - "Si aucun achievement, la page affiche un état vide encourageant (pas de crash)"
    - "Le badge dans la sidebar indique le nombre d'achievements récents (< 7 jours)"
  artifacts:
    - path: "src/app/(dashboard)/achievements/page.tsx"
      provides: "Page timeline — fetch GET /api/achievements, render liste styled inline CSS C.*"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "NAV_SECTIONS mis à jour avec /achievements et badge dynamique"
  key_links:
    - from: "src/app/(dashboard)/achievements/page.tsx"
      to: "/api/achievements"
      via: "fetch GET au montage"
      pattern: "fetch.*api/achievements"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "/achievements"
      via: "NAV_SECTIONS href"
      pattern: "href.*achievements"
---

<objective>
Créer la page timeline /achievements qui affiche l'historique des objectifs atteints, et mettre à jour la sidebar pour pointer vers cette page avec un badge dynamique.

Purpose: ACH-04 — L'utilisateur peut consulter ses succès passés avec dates. Slice vertical complet : layout (nav) + page + données API (déjà créée en 04-01).

Output: Page /achievements fonctionnelle, sidebar mise à jour avec lien et badge.
</objective>

<execution_context>
@C:/Users/Ted/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ted/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-achievements-artefacts/04-01-SUMMARY.md

<!-- Vérifier que 04-01-SUMMARY.md existe avant de commencer -->
<!-- Layout : src/app/(dashboard)/layout.tsx — 'use client', NAV_SECTIONS défini lignes 8-56 -->
<!-- Route GET /api/achievements créée en Plan 04-01 — retourne { data: { achievements: Achievement[] } } -->
<!-- C.* constantes : gold '#e8c878', textHi '#ffffff', text '#d8e1ff', surface1 '#11163a', line '#3a4690' -->

<interfaces>
<!-- Contrats issus du Plan 04-01 -->

Depuis src/types/achievements.ts (créé en 04-01):
```typescript
export type AchievementType = 'ca_monthly' | 'clients_milestone'

export interface Achievement {
  id: string
  user_id: string
  achievement_key: string
  achievement_type: AchievementType
  label: string
  value: number | null
  target: number | null
  achieved_at: string  // ISO string
  created_at: string
}
```

GET /api/achievements — réponse:
```typescript
{ data: { achievements: Achievement[] } }  // triés par achieved_at DESC
```

NAV_SECTIONS dans layout.tsx (extrait ligne 10-18):
```typescript
{ id: 'champions', href: '/champions', label: '🏆 Champions' },
// → Remplacer href par '/achievements' OU ajouter un item 'achievements'
// Décision : modifier l'item 'champions' pour pointer vers /achievements
// (la route /champions est dans la nav mais sans page implémentée — A3 assumption confirmée)
```

Couleurs C.* pertinentes:
```typescript
C.gold     = '#e8c878'   // titre, icônes trophée
C.textHi   = '#ffffff'   // label principal achievement
C.text     = '#d8e1ff'   // texte secondaire
C.textMid  = '#8ea0d9'   // date, métadonnées
C.surface1 = '#11163a'   // background carte
C.line     = '#3a4690'   // bordures
C.green    = '#4ade80'   // badge "ca_monthly" (objectif atteint)
C.warn     = '#d8884a'   // badge "clients_milestone"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : Page /achievements — timeline des succès</name>
  <files>src/app/(dashboard)/achievements/page.tsx</files>
  <action>
Créer la page Client Component qui fetch GET /api/achievements et affiche une timeline stylée inline CSS (thème PSG Cosmos, C.* constantes).

**Structure de la page :**

```typescript
'use client'

import { useEffect, useState } from 'react'
import { C } from '@/lib/theme'
import type { Achievement } from '@/types/achievements'

const TYPE_CONFIG: Record<string, { icon: string; color: string; bg: string }> = {
  ca_monthly: {
    icon: '🏆',
    color: C.green,
    bg: 'rgba(74,222,128,0.1)',
  },
  clients_milestone: {
    icon: '👥',
    color: C.warn,
    bg: 'rgba(216,136,74,0.1)',
  },
}

function formatDate(iso: string): string {
  return new Date(iso).toLocaleDateString('fr-FR', {
    day: 'numeric',
    month: 'long',
    year: 'numeric',
  })
}

function isRecent(iso: string): boolean {
  const diff = Date.now() - new Date(iso).getTime()
  return diff < 7 * 24 * 60 * 60 * 1000
}

export default function AchievementsPage() {
  const [achievements, setAchievements] = useState<Achievement[]>([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    fetch('/api/achievements')
      .then(r => r.json())
      .then(({ data }) => {
        setAchievements(data?.achievements ?? [])
      })
      .catch(() => setAchievements([]))
      .finally(() => setLoading(false))
  }, [])

  return (
    <div style={{ maxWidth: 720, margin: '0 auto', padding: '24px 0' }}>
      {/* En-tête */}
      <div style={{ marginBottom: 32 }}>
        <h1 style={{
          fontFamily: "'Oswald', sans-serif",
          fontSize: 28,
          fontWeight: 600,
          color: C.gold,
          letterSpacing: '0.04em',
          textShadow: '0 0 20px rgba(232,200,120,0.3)',
          marginBottom: 6,
        }}>
          Mes Succès
        </h1>
        <p style={{ fontSize: 13, color: C.textMid }}>
          {achievements.length > 0
            ? `${achievements.length} objectif${achievements.length > 1 ? 's' : ''} atteint${achievements.length > 1 ? 's' : ''}`
            : 'Vos badges et célébrations apparaîtront ici'}
        </p>
      </div>

      {/* État chargement */}
      {loading && (
        <div style={{ color: C.textMid, fontSize: 13, padding: '40px 0', textAlign: 'center' }}>
          Chargement...
        </div>
      )}

      {/* État vide */}
      {!loading && achievements.length === 0 && (
        <div style={{
          background: C.surface1,
          border: `1px solid ${C.line}`,
          borderRadius: 12,
          padding: '48px 24px',
          textAlign: 'center',
        }}>
          <div style={{ fontSize: 48, marginBottom: 16 }}>🎯</div>
          <div style={{ fontSize: 15, color: C.textHi, fontWeight: 500, marginBottom: 8 }}>
            Aucun badge encore débloqué
          </div>
          <div style={{ fontSize: 12, color: C.textMid }}>
            Atteignez votre objectif CA mensuel ou ajoutez 10 clients pour débloquer votre premier badge.
          </div>
        </div>
      )}

      {/* Timeline */}
      {!loading && achievements.length > 0 && (
        <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
          {achievements.map((a) => {
            const cfg = TYPE_CONFIG[a.achievement_type] ?? TYPE_CONFIG.ca_monthly
            const recent = isRecent(a.achieved_at)
            return (
              <div
                key={a.id}
                style={{
                  display: 'flex',
                  alignItems: 'flex-start',
                  gap: 16,
                  background: C.surface1,
                  border: `1px solid ${recent ? cfg.color : C.line}`,
                  borderRadius: 10,
                  padding: '16px 20px',
                  position: 'relative',
                  boxShadow: recent ? `0 0 12px ${cfg.color}22` : 'none',
                  transition: 'border-color 0.2s',
                }}
              >
                {/* Icône */}
                <div style={{
                  width: 44,
                  height: 44,
                  borderRadius: '50%',
                  background: cfg.bg,
                  border: `1px solid ${cfg.color}`,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontSize: 22,
                  flexShrink: 0,
                }}>
                  {cfg.icon}
                </div>

                {/* Contenu */}
                <div style={{ flex: 1, minWidth: 0 }}>
                  <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 4 }}>
                    <span style={{
                      fontSize: 14,
                      fontWeight: 600,
                      color: C.textHi,
                      fontFamily: "'Oswald', sans-serif",
                      letterSpacing: '0.02em',
                    }}>
                      {a.label}
                    </span>
                    {recent && (
                      <span style={{
                        fontSize: 9,
                        fontWeight: 700,
                        color: cfg.color,
                        background: cfg.bg,
                        border: `1px solid ${cfg.color}`,
                        borderRadius: 4,
                        padding: '1px 6px',
                        letterSpacing: '0.08em',
                        fontFamily: "'JetBrains Mono', monospace",
                        textTransform: 'uppercase',
                      }}>
                        Nouveau
                      </span>
                    )}
                  </div>
                  <div style={{ fontSize: 12, color: C.textMid }}>
                    {formatDate(a.achieved_at)}
                    {a.value != null && a.target != null && (
                      <span style={{ marginLeft: 12, color: C.textLo }}>
                        {a.achievement_type === 'ca_monthly'
                          ? `${a.value.toLocaleString('fr-FR')} € / ${a.target.toLocaleString('fr-FR')} €`
                          : `${a.value} / ${a.target} clients`}
                      </span>
                    )}
                  </div>
                </div>
              </div>
            )
          })}
        </div>
      )}
    </div>
  )
}
```

S'assurer que le répertoire `src/app/(dashboard)/achievements/` est créé avec le fichier `page.tsx`. Pas de fichier `layout.tsx` nécessaire — le layout parent `(dashboard)/layout.tsx` s'applique automatiquement.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const page=fs.readFileSync('src/app/(dashboard)/achievements/page.tsx','utf8'); console.assert(page.includes(\"'use client'\"), 'use client manquant'); console.assert(page.includes('api/achievements'), 'fetch manquant'); console.assert(page.includes('achievements.length === 0'), 'etat vide manquant'); console.assert(page.includes('formatDate'), 'formatage date manquant'); console.assert(page.includes('C.gold') || page.includes('C.'), 'theme C.* manquant'); console.log('OK')"</automated>
  </verify>
  <done>Navigation vers /achievements affiche la liste des achievements débloqués avec dates et icônes. État vide encourageant si aucun badge. Pas d'erreur TypeScript.</done>
</task>

<task type="auto">
  <name>Tache 2 : Sidebar — lien /achievements + badge compteur récent</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <action>
Modifier le layout pour que la sidebar pointe vers /achievements et affiche un badge numérique des achievements récents (< 7 jours).

Le layout est déjà un Client Component ('use client'). Le badge dynamique nécessite un fetch léger au montage.

**Modifications à apporter dans layout.tsx :**

1. Modifier l'item 'champions' dans NAV_SECTIONS pour pointer vers /achievements :
```typescript
// Avant :
{ id: 'champions', href: '/champions', label: '🏆 Champions' },
// Après :
{ id: 'achievements', href: '/achievements', label: '🏆 Champions' },
```

2. Ajouter un état pour le count d'achievements récents. Ajouter après les imports existants et les constantes NAV_SECTIONS :
```typescript
import { useEffect, useState } from 'react'
// Note : useState est déjà importé, ajouter useEffect si manquant
```

3. Dans le composant DashboardLayout, ajouter le state et le fetch :
```typescript
const [recentCount, setRecentCount] = useState<number>(0)

useEffect(() => {
  fetch('/api/achievements')
    .then(r => r.json())
    .then(({ data }) => {
      const achievements = data?.achievements ?? []
      const cutoff = Date.now() - 7 * 24 * 60 * 60 * 1000
      const recent = achievements.filter((a: { achieved_at: string }) =>
        new Date(a.achieved_at).getTime() > cutoff
      )
      setRecentCount(recent.length)
    })
    .catch(() => {/* silencieux */})
}, [])
```

4. Modifier le rendu de l'item badge dans la nav. Actuellement les badges sont statiques (`badge: 5`, `badge: 8`). Pour l'item achievements, injecter `recentCount` dynamiquement dans le rendu :

Dans la section `{item.badge && (...)`, le badge est rendu pour tous les items ayant `item.badge`. Pour l'item achievements, on veut afficher `recentCount` seulement si > 0.

La façon la plus simple est d'ajouter une condition dans le JSX de rendu des liens :

```typescript
// Dans la boucle items.map((item) => { ... })
// Après le rendu du label (span), ajouter AVANT la fermeture du Link :

{/* Badge statique (tâches, prospects) */}
{item.badge && item.id !== 'achievements' && (
  <span style={{
    minWidth: 16, height: 16,
    background: ribbon, color: '#0a0e22',
    borderRadius: 8, fontSize: 8, fontWeight: 700,
    display: 'flex', alignItems: 'center', justifyContent: 'center',
    padding: '0 4px', fontFamily: "'JetBrains Mono', monospace",
    boxShadow: '0 2px 8px rgba(200,64,72,0.45)',
  }}>
    {item.badge}
  </span>
)}
{/* Badge dynamique achievements */}
{item.id === 'achievements' && recentCount > 0 && (
  <span style={{
    minWidth: 16, height: 16,
    background: '#e8c878', color: '#0a0e22',
    borderRadius: 8, fontSize: 8, fontWeight: 700,
    display: 'flex', alignItems: 'center', justifyContent: 'center',
    padding: '0 4px', fontFamily: "'JetBrains Mono', monospace",
    boxShadow: '0 2px 8px rgba(232,200,120,0.5)',
  }}>
    {recentCount}
  </span>
)}
```

**Important :** Remplacer le bloc `{item.badge && (...)}` existant par ces deux blocs séparés. Le reste du layout (sidebar, topbar, AchievementsProvider, Script) ne doit pas être modifié.

**Vérifier que useEffect est bien importé** — si le layout avait seulement `useState`, ajouter `useEffect` dans l'import React : `import { useState, useEffect } from 'react'`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const layout=fs.readFileSync('src/app/(dashboard)/layout.tsx','utf8'); console.assert(layout.includes('href: \"/achievements\"') || layout.includes(\"href: '/achievements'\"), 'lien achievements manquant'); console.assert(layout.includes('recentCount'), 'state recentCount manquant'); console.assert(layout.includes('api/achievements'), 'fetch achievements manquant'); console.assert(layout.includes('AchievementsProvider'), 'AchievementsProvider toujours present'); console.assert(layout.includes('celebrations.js'), 'Script celebrations toujours present'); console.log('OK')"</automated>
  </verify>
  <done>Clic sur "🏆 Champions" dans la sidebar navigue vers /achievements. Si achievements récents (< 7 jours), un badge gold affiche le count. Badge disparaît après 7 jours.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> GET /api/achievements | Fetch depuis layout et page — authentification serveur obligatoire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | GET /api/achievements (layout fetch) | mitigate | Route protégée par getUser() — RLS Supabase en double filet |
| T-04-08 | Tampering | badge recentCount | accept | Donnée d'affichage cosmétique — aucun impact sécurité |
| T-04-09 | Denial of Service | fetch achievements au montage layout | accept | Une seule requête légère au montage — usage solo, pas de rate limit nécessaire |
</threat_model>

<verification>
Vérification fonctionnelle :

1. Naviguer vers /achievements depuis la sidebar
2. Si achievements existants en BDD : liste visible avec dates en français, icônes, badges "Nouveau" si < 7 jours
3. Si aucun achievement : message vide affiché (pas de crash, pas d'écran blanc)
4. Badge gold dans la sidebar visible si achievements récents (count > 0)
5. TypeScript : `npx tsc --noEmit` sans erreur sur les nouveaux fichiers
</verification>

<success_criteria>
- Page /achievements accessible depuis la sidebar (lien 🏆 Champions mis à jour)
- Timeline affiche achievements triés par date décroissante avec label, date fr-FR, et badge "Nouveau" si < 7 jours
- État vide encourageant si aucun badge débloqué
- Badge dynamique dans la sidebar (gold, count) si achievements récents
- Aucune régression dans le layout existant (Toaster, AchievementsProvider, Script celebrations.js toujours présents)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/04-achievements-artefacts/04-02-SUMMARY.md` avec :
- Confirmation navigation /achievements fonctionnelle
- État du badge sidebar (visible si recentCount > 0)
- Éventuelles divergences avec le plan (ex : item nav renommé, structure JSX différente)
- Couverture finale : ACH-01 ✓ ACH-02 ✓ ACH-03 ✓ ACH-04 ✓ ACH-05 ✓
</output>
