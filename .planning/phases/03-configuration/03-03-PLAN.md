---
phase: 03-configuration
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/app/(dashboard)/settings/page.tsx
autonomous: true
requirements:
  - CFG-04
  - CFG-05
  - CFG-08
  - CFG-09

must_haves:
  truths:
    - "L'onglet Triggers liste tous les templates avec leur pipeline_stage et leur toggle auto_trigger"
    - "Activer/désactiver un trigger depuis l'onglet Triggers persisté en base immédiatement"
    - "L'onglet Messages (onglet Notif existant ou onglet dédié) permet d'éditer les templates JSONB par canal et par stade"
    - "Sauvegarder un template de message déclenche un PATCH /api/settings et persiste en base"
    - "Rechargement de la page : templates de messages et état des triggers rechargés sans perte"
  artifacts:
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "TabTriggers (liste + toggles) + TabMessages (éditeur JSONB par canal/stade)"
      contains: "TabTriggers"
  key_links:
    - from: "settings/page.tsx (TabTriggers)"
      to: "/api/crm/sequences/templates PATCH auto_trigger"
      via: "fetch PATCH on Toggle change"
      pattern: "api/crm/sequences/templates"
    - from: "settings/page.tsx (TabMessages)"
      to: "/api/settings PATCH message_templates"
      via: "useUserSettings().save({ message_templates: merged })"
      pattern: "useUserSettings"
---

<objective>
Implémenter l'onglet Triggers (vue synthétique de tous les auto-triggers) et l'éditeur de templates de messages dans l'onglet Notifications/Messages de la page settings.

Purpose: Permettre à l'utilisateur d'activer/désactiver chaque trigger individuellement (CFG-05, CFG-08) et d'éditer les templates de messages par canal et par stade pipeline (CFG-04), avec persistance via /api/settings JSONB (CFG-09).
Output: TabTriggers + éditeur de messages fonctionnels dans settings/page.tsx.
</objective>

<execution_context>
@C:/Users/Ted/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ted/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-configuration/03-RESEARCH.md
@.planning/phases/03-configuration/03-01-SUMMARY.md
@.planning/phases/03-configuration/03-02-SUMMARY.md

<interfaces>
<!-- useUserSettings — créé en plan 03-01 -->
```typescript
import { useUserSettings, UserSettings } from '@/hooks/useUserSettings'
// settings.message_templates : Record<string, Record<string, string>>
// save({ message_templates: merged }) — merge côté API avant UPSERT
// Structure JSONB attendue :
// {
//   "whatsapp": { "a_contacter": "Bonjour {{prenom}}...", "rdv1": "...", "default": "..." },
//   "email":    { "a_contacter": "Objet:...", "default": "..." },
//   "sms":      { "default": "..." }
// }
```

<!-- PATCH auto_trigger — route créée en plan 03-02 -->
```typescript
// PATCH /api/crm/sequences/templates/{id}
// body: { auto_trigger: boolean }
// Réponse 409 si conflit (auto_trigger=true + stage déjà occupé)
// Réponse 200 avec template mis à jour
```

<!-- GET templates — retourne pipeline_stage + auto_trigger -->
```typescript
// GET /api/crm/sequences/templates
// { data: { templates: [{ id, name, pipeline_stage, auto_trigger }] } }
```

<!-- Composants settings existants — settings/page.tsx -->
```typescript
// SectionPanel, SetRow, SetLabel, Toggle, SetBtn — DÉJÀ définis dans le fichier
// C.* depuis @/lib/theme
// Pipeline stages :
const PIPELINE_STAGES_LABELS: Record<string, string> = {
  a_contacter: 'À contacter',
  rdv1: 'RDV R1',
  rdv2: 'RDV R2',
  rdv3: 'RDV R3',
  converti: 'Converti',
  perdu: 'Perdu',
}
const CHANNELS = ['whatsapp', 'email', 'sms'] as const
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TabTriggers — vue synthétique des auto-triggers</name>
  <files>src/app/(dashboard)/settings/page.tsx</files>
  <action>
Remplacer le placeholder `{activeTab === 'triggers' && <div>...placeholder...</div>}` par `<TabTriggers />`.

**Créer la fonction `TabTriggers()` dans `settings/page.tsx` :**

```typescript
function TabTriggers() {
  const [templates, setTemplates] = useState<Array<{
    id: string
    name: string
    pipeline_stage: string | null
    auto_trigger: boolean
  }>>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    fetch('/api/crm/sequences/templates')
      .then(r => r.json())
      .then(({ data }) => { setTemplates(data?.templates ?? []); setLoading(false) })
      .catch(() => setLoading(false))
  }, [])

  async function handleToggle(t: { id: string; name: string; pipeline_stage: string | null; auto_trigger: boolean }) {
    setError(null)
    const newVal = !t.auto_trigger
    // Optimistic update
    setTemplates(prev => prev.map(x => x.id === t.id ? { ...x, auto_trigger: newVal } : x))
    try {
      const res = await fetch(`/api/crm/sequences/templates/${t.id}`, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ auto_trigger: newVal }),
      })
      const { data, error: apiErr } = await res.json()
      if (apiErr || !res.ok) {
        // Rollback
        setTemplates(prev => prev.map(x => x.id === t.id ? { ...x, auto_trigger: t.auto_trigger } : x))
        setError(apiErr ?? `Erreur ${res.status}`)
        return
      }
      // Sync avec valeur serveur
      setTemplates(prev => prev.map(x => x.id === t.id ? { ...x, ...data.template } : x))
    } catch (e) {
      // Rollback en cas d'erreur réseau
      setTemplates(prev => prev.map(x => x.id === t.id ? { ...x, auto_trigger: t.auto_trigger } : x))
      setError('Erreur réseau')
    }
  }

  if (loading) return <div style={{ color: C.textLo, fontSize: 11, padding: 20 }}>Chargement...</div>

  return (
    <>
      <SectionPanel title="⚡ TRIGGERS AUTOMATIQUES PAR SÉQUENCE">
        <div style={{ fontSize: 9, color: C.textLo, marginBottom: 12, lineHeight: 1.6, fontFamily: 'JetBrains Mono,monospace' }}>
          Chaque trigger déclenche automatiquement la séquence associée quand un prospect entre dans le stade correspondant.
          Un seul trigger auto par stade pipeline est autorisé.
        </div>

        {templates.length === 0 && (
          <div style={{ color: C.textLo, fontSize: 10, padding: 12, textAlign: 'center', fontFamily: 'Inter,sans-serif' }}>
            Aucune séquence configurée — créez des templates dans l'onglet Séquences.
          </div>
        )}

        {templates.map(t => (
          <SetRow key={t.id}>
            <div style={{ flex: 1 }}>
              <SetLabel
                label={t.name}
                desc={t.pipeline_stage ? `Stade : ${PIPELINE_STAGES_LABELS[t.pipeline_stage] ?? t.pipeline_stage}` : 'Aucun stade assigné — trigger impossible'}
              />
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
              {t.pipeline_stage ? (
                <>
                  <span style={{ fontSize: 8, color: t.auto_trigger ? C.green : C.textVlo, fontFamily: 'JetBrains Mono,monospace' }}>
                    {t.auto_trigger ? 'ACTIF' : 'INACTIF'}
                  </span>
                  <Toggle
                    checked={t.auto_trigger}
                    onChange={() => handleToggle(t)}
                  />
                </>
              ) : (
                <span style={{ fontSize: 8, color: C.textVlo, fontFamily: 'JetBrains Mono,monospace' }}>
                  Assigner un stade d'abord
                </span>
              )}
            </div>
          </SetRow>
        ))}

        {error && (
          <div style={{ marginTop: 8, padding: 8, background: '#1f0d0d', border: `1px solid ${C.cyan}`, borderRadius: 6, color: C.cyan, fontSize: 9, fontFamily: 'JetBrains Mono,monospace' }}>
            {error}
          </div>
        )}
      </SectionPanel>

      <SectionPanel title="ℹ️ RÈGLES">
        <div style={{ fontSize: 9, color: C.textLo, lineHeight: 1.8, fontFamily: 'JetBrains Mono,monospace' }}>
          <div>• Un seul trigger auto par stade pipeline (contrainte système)</div>
          <div>• Désactiver un trigger n'annule pas les séquences déjà en cours</div>
          <div>• Configurer le stade d'un template dans l'onglet Séquences</div>
        </div>
      </SectionPanel>
    </>
  )
}
```

**Constante à ajouter en haut du fichier (au niveau module, avant les composants) :**
```typescript
const PIPELINE_STAGES_LABELS: Record<string, string> = {
  a_contacter: 'À contacter',
  rdv1: 'RDV R1',
  rdv2: 'RDV R2',
  rdv3: 'RDV R3',
  converti: 'Converti',
  perdu: 'Perdu',
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- `tsc --noEmit` sans erreur
- Onglet Triggers liste tous les templates avec leur stade et toggle
- Toggle → PATCH /api/crm/sequences/templates/{id} → état mis à jour
- Conflit auto_trigger → rollback + message d'erreur rouge inline
- Templates sans pipeline_stage : toggle désactivé avec message explicatif
  </done>
</task>

<task type="auto">
  <name>Task 2: Éditeur de templates de messages (JSONB) dans l'onglet Notifications</name>
  <files>src/app/(dashboard)/settings/page.tsx</files>
  <action>
Ajouter une section "Templates de messages" dans `TabNotifications` (après les canaux existants), en utilisant `useUserSettings`.

**Modifier `TabNotifications` pour recevoir `settings` et `save` en props** (passer depuis le composant parent, identique au pattern TabKPI du plan 03-01) :

```typescript
function TabNotifications({
  settings,
  save,
  saving,
}: {
  settings: UserSettings | null
  save: (p: Partial<UserSettings>) => Promise<unknown>
  saving: boolean
}) {
  // ... code existant des toggles Notif (pushOn, emailOn, etc.) INCHANGÉ ...

  // Nouvel état pour l'éditeur de messages
  const [selectedChannel, setSelectedChannel] = useState<'whatsapp' | 'email' | 'sms'>('whatsapp')
  const [selectedStage, setSelectedStage] = useState<string>('a_contacter')
  const [editedText, setEditedText] = useState('')
  const [msgSaving, setMsgSaving] = useState(false)

  // Initialiser editedText depuis settings quand channel/stage changent
  useEffect(() => {
    const current = settings?.message_templates?.[selectedChannel]?.[selectedStage] ?? ''
    setEditedText(current)
  }, [selectedChannel, selectedStage, settings])

  async function saveMessageTemplate() {
    if (!settings) return
    setMsgSaving(true)
    try {
      // Construire le patch JSONB — merger avec l'existant côté client
      // La route API /api/settings PATCH merge côté serveur aussi (protection double)
      const channelTemplates = { ...(settings.message_templates?.[selectedChannel] ?? {}), [selectedStage]: editedText }
      const merged = { ...(settings.message_templates ?? {}), [selectedChannel]: channelTemplates }
      await save({ message_templates: merged })
      toast.success('Template de message enregistré')
    } catch (e) {
      toast.error('Erreur lors de la sauvegarde')
    } finally {
      setMsgSaving(false)
    }
  }

  // ... retour JSX ...
}
```

**Section à ajouter dans le JSX de TabNotifications, après les SectionPanel existants :**

```typescript
<SectionPanel title="📝 TEMPLATES DE MESSAGES">
  <div style={{ fontSize: 9, color: C.textLo, marginBottom: 12, lineHeight: 1.6, fontFamily: 'JetBrains Mono,monospace' }}>
    Personnalise le message envoyé pour chaque canal et chaque stade pipeline.
    Variables disponibles : {'{{prenom}}'}, {'{{nom}}'}, {'{{email}}'}, {'{{telephone}}'}
  </div>

  {/* Sélecteur canal */}
  <div style={{ display: 'flex', gap: 6, marginBottom: 10 }}>
    {(['whatsapp', 'email', 'sms'] as const).map(ch => (
      <button
        key={ch}
        onClick={() => setSelectedChannel(ch)}
        style={{
          padding: '5px 12px', borderRadius: 5, fontSize: 9, cursor: 'pointer',
          fontFamily: 'Oswald,sans-serif', letterSpacing: '0.05em',
          background: selectedChannel === ch ? '#1a1400' : C.surface2,
          color: selectedChannel === ch ? C.gold : C.textLo,
          border: `1px solid ${selectedChannel === ch ? C.gold : C.line}`,
        }}
      >
        {ch === 'whatsapp' ? '💬 WhatsApp' : ch === 'email' ? '📧 Email' : '📱 SMS'}
      </button>
    ))}
  </div>

  {/* Sélecteur stade */}
  <div style={{ marginBottom: 10 }}>
    <select
      value={selectedStage}
      onChange={e => setSelectedStage(e.target.value)}
      style={{
        width: '100%', padding: '6px 8px', background: C.surface2,
        border: `1px solid ${C.line}`, borderRadius: 5,
        color: C.text, fontSize: 10, fontFamily: 'Inter,sans-serif',
      }}
    >
      {Object.entries(PIPELINE_STAGES_LABELS).map(([val, label]) => (
        <option key={val} value={val}>{label}</option>
      ))}
      <option value="default">Défaut (tous stades)</option>
    </select>
  </div>

  {/* Éditeur textarea */}
  <textarea
    value={editedText}
    onChange={e => setEditedText(e.target.value)}
    placeholder={`Template ${selectedChannel} pour ${selectedStage}...\nEx: Bonjour {{prenom}}, suite à notre échange...`}
    style={{
      width: '100%', minHeight: 120, padding: 10,
      background: C.surface2, border: `1px solid ${C.line}`,
      borderRadius: 6, color: C.text, fontSize: 10,
      lineHeight: 1.6, resize: 'vertical', fontFamily: 'Inter,sans-serif',
      boxSizing: 'border-box',
    }}
  />

  {/* Indicateur longueur pour SMS */}
  {selectedChannel === 'sms' && (
    <div style={{ fontSize: 8, color: editedText.length > 160 ? C.warn : C.textLo, marginTop: 4, fontFamily: 'JetBrains Mono,monospace' }}>
      {editedText.length}/160 caractères {editedText.length > 160 ? '(SMS multiple)' : ''}
    </div>
  )}

  {/* Bouton Enregistrer */}
  <div style={{ marginTop: 10, display: 'flex', gap: 8, alignItems: 'center' }}>
    <SetBtn color={C.green} bg="#0d1a0d" onClick={saveMessageTemplate}>
      {msgSaving ? '⏳ Enregistrement...' : '💾 Enregistrer le template'}
    </SetBtn>
    {/* Preview valeur actuelle en base */}
    {settings?.message_templates?.[selectedChannel]?.[selectedStage] && (
      <span style={{ fontSize: 8, color: C.textLo, fontFamily: 'JetBrains Mono,monospace' }}>
        ✓ Template existant chargé
      </span>
    )}
  </div>
</SectionPanel>
```

**Dans le composant parent (le composant page principal), passer `settings` et `save` à TabNotifications :**
```typescript
{activeTab === 'notifications' && <TabNotifications settings={settings} save={save} saving={saving} />}
```
(Même pattern que TabKPI du plan 03-01 — `settings` et `save` viennent de `useUserSettings()` instancié dans le parent.)

**Toast import :** Vérifier que `import { toast } from 'sonner'` est présent (ajouté en plan 03-01 si nécessaire — si absent, ajouter).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- `tsc --noEmit` sans erreur
- Onglet Notifications : section "Templates de messages" visible en bas
- Sélectionner canal WhatsApp + stade "À contacter" → textarea vide (ou valeur précédemment sauvée)
- Taper un texte + cliquer Enregistrer → toast "Template de message enregistré"
- Rechargement de la page → même texte rechargé dans le textarea (valeur persistée en base)
- SMS : compteur de caractères affiché, alerte si > 160
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/settings PATCH message_templates | Contenu libre (texte des templates) — stocké tel quel sans exécution |
| Client → /api/crm/sequences/templates/{id} PATCH | Toggle auto_trigger — conflict check côté serveur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03C-01 | Tampering | message_templates JSONB écrasement | mitigate | Merge côté serveur (GET existing + spread) avant UPSERT — double protection client + serveur |
| T-03C-02 | Tampering | XSS via message_template text | accept | Texte stocké as-is et affiché dans textarea — jamais rendu en HTML dangerouslySetInnerHTML |
| T-03C-03 | Spoofing | Toggle auto_trigger sans pipeline_stage | mitigate | UI désactive le toggle si pipeline_stage null — API vérifie conflict check côté serveur |
| T-03C-04 | Denial of Service | Textarea texte arbitrairement long | accept | Usage solo local — pas de quota en base MVP |
</threat_model>

<verification>
- `npx tsc --noEmit` : 0 erreur TypeScript
- Smoke UI : onglet Triggers → liste templates + toggles opérationnels
- Smoke UI : onglet Notifications → éditeur messages → save → rechargement → valeur persistée
- Conflit auto_trigger sur onglet Triggers : rollback + erreur inline (pas de page blanche)
- Tous les styles via `C.*` — grep vérification : `grep -v '^//' src/app/(dashboard)/settings/page.tsx | grep 'className='` doit retourner vide ou uniquement des classes non-couleur
</verification>

<success_criteria>
- CFG-04 : templates messages par canal (whatsapp/email/sms) et stade persistés en JSONB
- CFG-05 : pipeline_stage affiché (lecture seule) dans l'onglet Triggers — la modification est dans le plan 03-02 (onglet Séquences)
- CFG-08 : toggle auto_trigger individuel depuis TabTriggers, rollback si conflit
- CFG-09 : rechargement page → settings rechargées sans perte
- 0 erreur TypeScript
- 0 classe Tailwind directe dans le code modifié (uniquement `style={{ }}` avec `C.*`)
</success_criteria>

<output>
Après exécution, créer `.planning/phases/03-configuration/03-03-SUMMARY.md`
</output>
