---
phase: 03-configuration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/settings/route.ts
  - src/hooks/useUserSettings.ts
  - src/app/(dashboard)/settings/page.tsx
autonomous: true
requirements:
  - CFG-06
  - CFG-07
  - CFG-09

must_haves:
  truths:
    - "GET /api/settings retourne les valeurs par défaut si aucune row n'existe (code PGRST116 géré)"
    - "PATCH /api/settings persiste ca_monthly_target, ca_annual_target et client_health_threshold_days"
    - "L'onglet KPI de la page settings charge les valeurs réelles depuis l'API au montage"
    - "Modifier un seuil et cliquer Enregistrer persiste la valeur — rechargement de page conserve la nouvelle valeur"
  artifacts:
    - path: "src/app/api/settings/route.ts"
      provides: "GET + PATCH user_settings avec UPSERT et defaults PGRST116"
      exports: ["GET", "PATCH"]
    - path: "src/hooks/useUserSettings.ts"
      provides: "Hook client fetch + save avec état local"
      exports: ["useUserSettings", "UserSettings"]
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "TabKPI branché sur useUserSettings (ca_monthly_target, ca_annual_target, client_health_threshold_days)"
      contains: "useUserSettings"
  key_links:
    - from: "src/app/(dashboard)/settings/page.tsx (TabKPI)"
      to: "/api/settings"
      via: "useUserSettings hook — fetch GET au mount, PATCH on save"
      pattern: "useUserSettings"
    - from: "src/app/api/settings/route.ts"
      to: "supabase user_settings"
      via: "upsert({ id: user.id, ...body })"
      pattern: "upsert.*user_settings"
---

<objective>
Créer la route API `/api/settings` (GET + PATCH) et le hook `useUserSettings`, puis brancher l'onglet KPI de la page settings sur ces données réelles.

Purpose: Permettre à l'utilisateur de définir ses seuils KPI (CA mensuel, CA annuel, inactivité) et que ces valeurs soient persistées en base et rechargées au démarrage (CFG-06, CFG-07, CFG-09).
Output: Route API fonctionnelle + hook réutilisable + onglet KPI persistant.
</objective>

<execution_context>
@C:/Users/Ted/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ted/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-configuration/03-RESEARCH.md

<interfaces>
<!-- Pattern route API établi Phase 2 — src/app/api/revenue/stats/route.ts -->
```typescript
import { NextRequest } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { apiSuccess, apiError, apiUnauthorized } from '@/lib/api'

// TOUJOURS getUser() — jamais getSession()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return apiUnauthorized()
```

<!-- user_settings — schéma vérifié migration 001 -->
<!-- PK = id uuid references auth.users(id) — PAS un user_id séparé -->
<!-- Colonnes CFG-06/07/09 : -->
```sql
ca_monthly_target  numeric(12,2) default 15000
ca_annual_target   numeric(12,2) default 180000
client_health_threshold_days integer default 90
closing_target_pct numeric(5,2) default 40.0
calls_per_day_target integer default 20
rdv_per_week_target integer default 5
```

<!-- C.* depuis src/lib/theme.ts — OBLIGATOIRE pour tout style -->
```typescript
import { C } from '@/lib/theme'
// C.gold, C.green, C.surface1, C.surface2, C.line, C.lineSoft,
// C.textHi, C.text, C.textLo, C.textVlo, C.bgMid, C.bgDeep
// JAMAIS className="bg-gray-900" — toujours style={{ background: C.surface1 }}
```

<!-- Zod v4 — CRITIQUE -->
```typescript
import { z } from 'zod'
// .issues PAS .errors
const parsed = Schema.safeParse(body)
if (!parsed.success) return apiError(parsed.error.issues[0].message, 400)
```

<!-- Composants settings existants à réutiliser — settings/page.tsx -->
```typescript
// SectionPanel, SetRow, SetLabel, NumInput, Toggle, SetBtn
// sont DÉJÀ définis dans settings/page.tsx — ne pas les redéfinir
// NumInput est STATELESS — il faut le rendre controllé pour le brancher sur useUserSettings
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Route API /api/settings (GET + PATCH)</name>
  <files>src/app/api/settings/route.ts</files>
  <action>
Créer `src/app/api/settings/route.ts` avec GET et PATCH.

**GET :**
- `createSupabaseServerClient()` + `getUser()` → 401 si absent
- `.from('user_settings').select('*').eq('id', user.id).single()`
- Si `error.code === 'PGRST116'` → retourner `apiSuccess(getDefaultSettings())` (row absente = utilisateur nouveau)
- Toute autre erreur → `apiError(error.message)`
- Succès → `apiSuccess(data)`

**`getDefaultSettings()` function (à exporter) :**
```typescript
export function getDefaultSettings() {
  return {
    ca_monthly_target: 15000,
    ca_annual_target: 180000,
    client_health_threshold_days: 90,
    closing_target_pct: 40.0,
    calls_per_day_target: 20,
    rdv_per_week_target: 5,
    blocks_per_day_target: 6,
    message_templates: {} as Record<string, Record<string, string>>,
  }
}
```

**PATCH :**
- `getUser()` + Zod v4 validation du body
- Schéma Zod (tous optionnels) :
  ```typescript
  const PatchSettingsSchema = z.object({
    ca_monthly_target: z.number().positive().optional(),
    ca_annual_target: z.number().positive().optional(),
    client_health_threshold_days: z.number().int().min(1).max(365).optional(),
    closing_target_pct: z.number().min(0).max(100).optional(),
    calls_per_day_target: z.number().int().min(0).optional(),
    rdv_per_week_target: z.number().int().min(0).optional(),
    blocks_per_day_target: z.number().int().min(0).optional(),
    message_templates: z.record(z.record(z.string())).optional(),
  })
  ```
- Si `message_templates` dans le body : merger JSONB côté serveur (fetch existing + spread) avant UPSERT
- UPSERT : `.from('user_settings').upsert({ id: user.id, ...parsed.data, updated_at: new Date().toISOString() }).eq('id', user.id).select().single()`
- `.issues[0].message` pour les erreurs Zod — PAS `.errors`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- `GET /api/settings` retourne 200 avec les defaults si aucune row en base
- `PATCH /api/settings` avec `{ ca_monthly_target: 20000 }` retourne 200 avec la valeur mise à jour
- `tsc --noEmit` sans erreur sur ce fichier
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hook useUserSettings + brancher onglet KPI</name>
  <files>src/hooks/useUserSettings.ts, src/app/(dashboard)/settings/page.tsx</files>
  <action>
**Créer `src/hooks/useUserSettings.ts` :**

```typescript
'use client'
import { useEffect, useState, useCallback } from 'react'
import { getDefaultSettings } from '@/app/api/settings/route'  // type uniquement

export type UserSettings = ReturnType<typeof import('@/app/api/settings/route').getDefaultSettings>

export function useUserSettings() {
  const [settings, setSettings] = useState<UserSettings | null>(null)
  const [loading, setLoading] = useState(true)
  const [saving, setSaving] = useState(false)

  useEffect(() => {
    fetch('/api/settings')
      .then(r => r.json())
      .then(({ data }) => { setSettings(data); setLoading(false) })
      .catch(() => setLoading(false))
  }, [])

  const save = useCallback(async (partial: Partial<UserSettings>) => {
    setSaving(true)
    try {
      const res = await fetch('/api/settings', {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(partial),
      })
      const { data, error } = await res.json()
      if (error) throw new Error(error)
      setSettings(prev => prev ? { ...prev, ...data } : data)
      return data
    } finally {
      setSaving(false)
    }
  }, [])

  return { settings, loading, saving, save }
}
```

Note: Si l'import de `getDefaultSettings` crée une dépendance circulaire (hook client → route serveur), définir `UserSettings` directement dans le hook sans import de la route.

**Modifier `src/app/(dashboard)/settings/page.tsx` :**

1. Ajouter `'sequences' | 'triggers'` au type `Tab` :
   ```typescript
   type Tab = 'general' | 'kpi' | 'notifications' | 'integrations' | 'sections' | 'mobile' | 'sequences' | 'triggers'
   ```
   Et les 2 entrées dans `TABS` :
   ```typescript
   { id: 'sequences', label: '🔗 Séquences' },
   { id: 'triggers', label: '⚡ Triggers' },
   ```
   Changer le `gridTemplateColumns` de la grille tabs de `repeat(6,1fr)` à `repeat(8,1fr)`.

2. Importer `useUserSettings` dans la page (en haut, après les imports existants) :
   ```typescript
   import { useUserSettings } from '@/hooks/useUserSettings'
   ```

3. Dans `TabKPI`, recevoir `settings` et `save` en props :
   ```typescript
   function TabKPI({ settings, save, saving }: { settings: UserSettings | null; save: (p: Partial<UserSettings>) => Promise<unknown>; saving: boolean })
   ```

4. Dans `TabKPI`, remplacer les deux champs de la section `💰 Collecte` :
   - `ca_monthly_target` : input controllé sur `settings?.ca_monthly_target ?? 15000`
   - `ca_annual_target` : input controllé sur `settings?.ca_annual_target ?? 180000`
   Ces champs utilisent `useState` local initialisé depuis settings, puis le bouton "Enregistrer" de la section appelle `save({ ca_monthly_target, ca_annual_target })` + toast Sonner.

5. Dans `TabKPI`, section `📅 Rendez-vous` existante : ajouter le champ `client_health_threshold_days` (seuil inactivité, déjà dans la section KPI ou créer une `SectionPanel` dédiée `⚠️ Seuil d'inactivité`) :
   ```typescript
   <SectionPanel title="⚠️ Seuil d'inactivité client">
     <SetRow>
       <SetLabel label="Jours sans contact" desc="Client marqué inactif après X jours (défaut : 90)" />
       <NumInput ... /> // controllé, onSave: save({ client_health_threshold_days: val })
     </SetRow>
   </SectionPanel>
   ```

6. **NumInput doit accepter `onChange`** — le composant actuel n'a pas de prop `onChange`. Ajouter `onChange?: (v: number) => void` et appeler `onChange(Number(e.target.value))` en plus du `setVal`.

7. Dans le composant principal `SettingsPage` (ou équivalent), instancier `useUserSettings()` et passer `settings`, `save`, `saving` à `TabKPI`.

8. Ajouter les placeholders pour les 2 nouveaux onglets dans le rendu :
   ```typescript
   {activeTab === 'sequences' && <div style={{ color: C.textLo, fontSize: 11, padding: 20, fontFamily: 'JetBrains Mono,monospace' }}>Onglet Séquences — disponible plan 03-02</div>}
   {activeTab === 'triggers' && <div style={{ color: C.textLo, fontSize: 11, padding: 20, fontFamily: 'JetBrains Mono,monospace' }}>Onglet Triggers — disponible plan 03-03</div>}
   ```

**Toast feedback :** Utiliser `toast.success('Seuils KPI enregistrés')` depuis `sonner` (déjà dans le layout) après un save réussi. Ajouter `import { toast } from 'sonner'` si pas déjà présent.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- `tsc --noEmit` passe sans erreur
- La page `/settings` se charge, onglet KPI visible
- Les champs CA mensuel, CA annuel et seuil d'inactivité affichent les valeurs depuis l'API
- Modifier un champ + cliquer Enregistrer : toast "Seuils KPI enregistrés" apparaît
- Rechargement de la page : les nouvelles valeurs sont rechargées depuis Supabase
- 2 nouveaux onglets (Séquences, Triggers) visibles — affichent placeholder
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser → /api/settings | Corps de requête non fiable — valeurs numériques à valider |
| /api/settings → Supabase | RLS user_settings : auth.uid() = id garantit isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03A-01 | Tampering | PATCH /api/settings | mitigate | Zod v4 valide tous les champs — `.positive()`, `.min()/.max()` bloquent valeurs aberrantes |
| T-03A-02 | Tampering | user_settings row d'un autre user | accept | RLS Supabase `auth.uid() = id` — impossible via client standard |
| T-03A-03 | Spoofing | getUser() absent | mitigate | `apiUnauthorized()` sur user null — pattern établi Phases 0-2 |
| T-03A-04 | Information Disclosure | PGRST116 leak | accept | Seul le code d'erreur est vérifié — message interne non exposé au client |
</threat_model>

<verification>
- `npx tsc --noEmit` : 0 erreur
- `GET /api/settings` (navigateur authentifié) : retourne `{ data: { ca_monthly_target: 15000, ... } }`
- `PATCH /api/settings` body `{ ca_monthly_target: 20000 }` : retourne 200 avec nouvelle valeur
- Rechargement `/settings` → onglet KPI → valeur 20000 visible
- Onglets Séquences et Triggers visibles dans la barre de navigation settings
</verification>

<success_criteria>
- CFG-06 : ca_monthly_target et ca_annual_target persistés et rechargés
- CFG-07 : client_health_threshold_days persisté et rechargé
- CFG-09 : rechargement page sans perte de configuration
- 0 erreur TypeScript
- Aucune classe Tailwind directe dans le code modifié (uniquement `style={{ ... }}` avec `C.*`)
</success_criteria>

<output>
Après exécution, créer `.planning/phases/03-configuration/03-01-SUMMARY.md`
</output>
