---
phase: 03-configuration
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/api/crm/sequences/templates/route.ts
  - src/app/api/crm/sequences/templates/[id]/route.ts
  - src/app/api/crm/sequences/templates/[id]/steps/route.ts
  - src/app/api/crm/sequences/templates/[id]/steps/[stepId]/route.ts
  - src/app/(dashboard)/settings/page.tsx
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-05
  - CFG-08
  - CFG-09

must_haves:
  truths:
    - "L'utilisateur peut créer un nouveau template de séquence depuis l'onglet Séquences"
    - "L'utilisateur peut renommer un template et changer son pipeline_stage"
    - "L'utilisateur peut supprimer un template (steps supprimés par cascade)"
    - "L'utilisateur peut ajouter une étape (channel, delay_days) à un template"
    - "L'utilisateur peut modifier ou supprimer une étape existante"
    - "Tenter d'activer auto_trigger sur un stage déjà occupé produit une erreur claire"
    - "L'onglet Séquences remplace le placeholder du plan 03-01"
  artifacts:
    - path: "src/app/api/crm/sequences/templates/route.ts"
      provides: "GET (existant) + POST (nouveau template)"
      exports: ["GET", "POST"]
    - path: "src/app/api/crm/sequences/templates/[id]/route.ts"
      provides: "PATCH (nom/stage/auto_trigger) + DELETE template"
      exports: ["PATCH", "DELETE"]
    - path: "src/app/api/crm/sequences/templates/[id]/steps/route.ts"
      provides: "GET steps + POST nouvelle step"
      exports: ["GET", "POST"]
    - path: "src/app/api/crm/sequences/templates/[id]/steps/[stepId]/route.ts"
      provides: "PATCH + DELETE step"
      exports: ["PATCH", "DELETE"]
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "TabSequences — liste templates + steps CRUD inline"
      contains: "TabSequences"
  key_links:
    - from: "settings/page.tsx (TabSequences)"
      to: "/api/crm/sequences/templates"
      via: "fetch GET au mount + POST/PATCH/DELETE on actions"
      pattern: "api/crm/sequences/templates"
    - from: "PATCH template auto_trigger=true"
      to: "conflict check user_id + pipeline_stage"
      via: "SELECT count avant UPSERT"
      pattern: "auto_trigger.*conflict"
---

<objective>
Créer les routes API CRUD manquantes pour `sequence_templates` et `sequence_template_steps`, puis implémenter l'onglet Séquences dans la page settings pour remplacer le placeholder.

Purpose: L'utilisateur peut gérer visuellement ses séquences par stade pipeline — créer, éditer, supprimer templates et étapes — depuis la page Paramètres (CFG-01, CFG-02, CFG-03, CFG-05, CFG-08).
Output: 4 routes API + onglet Séquences fonctionnel avec CRUD complet.
</objective>

<execution_context>
@C:/Users/Ted/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ted/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-configuration/03-RESEARCH.md
@.planning/phases/03-configuration/03-01-SUMMARY.md

<interfaces>
<!-- Route GET existante à étendre — src/app/api/crm/sequences/templates/route.ts -->
```typescript
import { NextRequest } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { apiSuccess, apiError, apiUnauthorized } from '@/lib/api'

export async function GET(_req: NextRequest) {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return apiUnauthorized()
  const { data: templates, error } = await supabase
    .from('sequence_templates')
    .select('id, name, pipeline_stage, auto_trigger')
    .eq('user_id', user.id)
    .order('name', { ascending: true })
  if (error) return apiError(error.message)
  return apiSuccess({ templates: templates ?? [] })
}
```

<!-- Schéma sequence_templates — migration 005 vérifié -->
```typescript
// sequence_templates colonnes éditables :
// id uuid PK, user_id uuid NOT NULL, name text NOT NULL
// pipeline_stage pipeline_stage (enum: 'a_contacter'|'rdv1'|'rdv2'|'rdv3'|'converti'|'perdu')
// auto_trigger boolean default false
// created_at, updated_at

// sequence_template_steps colonnes éditables :
// id uuid PK, template_id uuid FK (cascade delete), step_order integer NOT NULL
// channel sequence_channel ('whatsapp'|'email'|'sms'|'call_reminder'|'linkedin')
// delay_days integer default 0, message_template text
// UNIQUE (template_id, step_order)
```

<!-- Contrainte critique auto_trigger — recherche de conflit avant PATCH -->
```typescript
// Avant de sauver auto_trigger = true sur template X pour stage S :
const { count } = await supabase
  .from('sequence_templates')
  .select('*', { count: 'exact', head: true })
  .eq('user_id', user.id)
  .eq('pipeline_stage', stage)
  .eq('auto_trigger', true)
  .neq('id', templateId) // exclure le template courant
if (count && count > 0) {
  return apiError(`Un trigger auto existe déjà pour le stade ${stage}. Désactivez-le d'abord.`, 409)
}
```

<!-- Zod v4 patterns — .issues PAS .errors -->
```typescript
import { z } from 'zod'
const PIPELINE_STAGES = ['a_contacter','rdv1','rdv2','rdv3','converti','perdu'] as const
const CHANNELS = ['whatsapp','email','sms','call_reminder','linkedin'] as const

const PatchTemplateSchema = z.object({
  name: z.string().min(1).optional(),
  pipeline_stage: z.enum(PIPELINE_STAGES).nullable().optional(),
  auto_trigger: z.boolean().optional(),
})
const PostStepSchema = z.object({
  channel: z.enum(CHANNELS),
  delay_days: z.number().int().min(0).max(365),
  message_template: z.string().optional(),
  step_order: z.number().int().min(1),
})
```

<!-- Composants settings existants — settings/page.tsx -->
```typescript
// SectionPanel, SetRow, SetLabel, Toggle, SetBtn — DÉJÀ définis, réutiliser
// useUserSettings import depuis @/hooks/useUserSettings (plan 03-01)
// C.* depuis @/lib/theme
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Routes CRUD templates + steps (4 fichiers API)</name>
  <files>
    src/app/api/crm/sequences/templates/route.ts,
    src/app/api/crm/sequences/templates/[id]/route.ts,
    src/app/api/crm/sequences/templates/[id]/steps/route.ts,
    src/app/api/crm/sequences/templates/[id]/steps/[stepId]/route.ts
  </files>
  <action>
**1. Étendre `src/app/api/crm/sequences/templates/route.ts` — ajouter POST :**

Ajouter `POST` à la fin du fichier existant (ne pas toucher le GET existant) :
- `getUser()` + Zod : `z.object({ name: z.string().min(1), pipeline_stage: z.enum(PIPELINE_STAGES).nullable().optional() })`
- INSERT : `.from('sequence_templates').insert({ user_id: user.id, name, pipeline_stage: pipeline_stage ?? null }).select().single()`
- Retourner `apiSuccess({ template: data })`

**2. Créer `src/app/api/crm/sequences/templates/[id]/route.ts` :**

```typescript
// PATCH — modifier nom, pipeline_stage, auto_trigger
export async function PATCH(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  // getUser() + Zod PatchTemplateSchema
  // Si auto_trigger = true dans parsed.data ET pipeline_stage présent :
  //   -> vérifier conflit (voir interfaces ci-dessus) -> 409 si conflit
  // Si auto_trigger = true et pipeline_stage absent :
  //   -> récupérer pipeline_stage actuel du template avant de vérifier conflit
  // PATCH : .from('sequence_templates').update(parsed.data).eq('id', id).eq('user_id', user.id).select().single()
  // .eq('user_id') = protection IDOR essentielle
}

// DELETE — supprimer template (steps supprimés par cascade)
export async function DELETE(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  // getUser()
  // .from('sequence_templates').delete().eq('id', id).eq('user_id', user.id)
  // retourner apiSuccess({ deleted: true })
}
```

**3. Créer `src/app/api/crm/sequences/templates/[id]/steps/route.ts` :**

```typescript
// GET — lister steps d'un template
export async function GET(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  // getUser() + vérifier que le template appartient à user (SELECT user_id)
  // .from('sequence_template_steps').select('*').eq('template_id', id).order('step_order')
  // retourner apiSuccess({ steps })
}

// POST — ajouter une étape
export async function POST(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  // getUser() + vérifier ownership template
  // Zod PostStepSchema
  // INSERT .from('sequence_template_steps').insert({ template_id: id, ...parsed.data }).select().single()
  // Gérer violation UNIQUE (template_id, step_order) : code '23505' → apiError('step_order déjà utilisé', 409)
}
```

**4. Créer `src/app/api/crm/sequences/templates/[id]/steps/[stepId]/route.ts` :**

```typescript
// PATCH — modifier channel, delay_days, message_template, step_order
// Schéma Zod : tous optionnels, mêmes types que PostStepSchema
// .from('sequence_template_steps').update(parsed.data).eq('id', stepId).select().single()
// Vérifier ownership : joindre via template_id → template.user_id = user.id
// (SELECT template_id d'abord, puis vérifier template.user_id)

// DELETE — supprimer step
// .from('sequence_template_steps').delete().eq('id', stepId)
// + vérifier ownership via template
```

**Règle transversale (toutes les routes) :**
- `getUser()` au début de chaque handler — jamais `getSession()`
- `.eq('user_id', user.id)` sur toutes les queries templates — protection IDOR
- `.issues[0].message` pour erreurs Zod (jamais `.errors`)
- `params` est une `Promise` en Next.js 15 — toujours `await params`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- `tsc --noEmit` sans erreur sur les 4 nouveaux fichiers
- `POST /api/crm/sequences/templates` body `{ name: "Test SEQ" }` retourne 201 avec l'objet créé
- `PATCH /api/crm/sequences/templates/{id}` body `{ auto_trigger: true, pipeline_stage: "rdv1" }` :
  - Retourne 200 si aucun autre template auto sur rdv1
  - Retourne 409 si un autre template a déjà auto_trigger=true sur rdv1
- `DELETE /api/crm/sequences/templates/{id}` retourne 200
- `POST /api/crm/sequences/templates/{id}/steps` body `{ channel: "email", delay_days: 2, step_order: 1 }` retourne 201
  </done>
</task>

<task type="auto">
  <name>Task 2: Onglet Séquences dans settings/page.tsx</name>
  <files>src/app/(dashboard)/settings/page.tsx</files>
  <action>
Remplacer le placeholder `{activeTab === 'sequences' && <div>...placeholder...</div>}` par `<TabSequences />`.

**Créer la fonction `TabSequences()` dans `settings/page.tsx` :**

Structure générale :
```typescript
type TemplateWithSteps = {
  id: string
  name: string
  pipeline_stage: string | null
  auto_trigger: boolean
  steps: Array<{
    id: string
    step_order: number
    channel: string
    delay_days: number
    message_template: string | null
  }>
}

function TabSequences() {
  const [templates, setTemplates] = useState<TemplateWithSteps[]>([])
  const [loading, setLoading] = useState(true)
  const [expandedId, setExpandedId] = useState<string | null>(null)
  const [creating, setCreating] = useState(false)
  const [newName, setNewName] = useState('')
  const [error, setError] = useState<string | null>(null)

  // Au mount : charger templates + steps pour chaque template
  useEffect(() => {
    loadTemplates()
  }, [])

  async function loadTemplates() { /* GET /api/crm/sequences/templates */ }
  async function loadSteps(templateId: string) { /* GET .../steps */ }
  async function createTemplate() { /* POST + reload */ }
  async function deleteTemplate(id: string) { /* DELETE + reload */ }
  async function toggleAutoTrigger(t: TemplateWithSteps) { /* PATCH auto_trigger */ }
  async function addStep(templateId: string) { /* POST step avec step_order = steps.length + 1 */ }
  async function deleteStep(templateId: string, stepId: string) { /* DELETE step */ }
  async function patchStep(stepId: string, patch: object) { /* PATCH step */ }
  ...
}
```

**UI — structure attendue :**

```
<SectionPanel title="🔗 SÉQUENCES PAR STADE PIPELINE">
  {/* Bouton Nouveau template */}
  <SetBtn color={C.green} bg="#0d1a0d" onClick={() => setCreating(true)}>+ Nouveau template</SetBtn>

  {creating && (
    <div>
      <input value={newName} onChange={...} placeholder="Nom du template..." style={...C.surface2...} />
      <SetBtn onClick={createTemplate}>Créer</SetBtn>
      <SetBtn onClick={() => setCreating(false)}>Annuler</SetBtn>
    </div>
  )}

  {templates.map(t => (
    <div key={t.id} style={{ border: `1px solid ${C.line}`, borderRadius: 8, marginBottom: 8 }}>
      {/* Header template */}
      <SetRow>
        <div style={{ flex: 1 }}>
          {/* Nom éditable — double-click ou input inline */}
          <span style={{ color: C.textHi, fontSize: 11 }}>{t.name}</span>
          {/* Pipeline stage select */}
          <select value={t.pipeline_stage ?? ''} onChange={e => patchTemplate(t.id, { pipeline_stage: e.target.value || null })}
            style={{ marginLeft: 8, background: C.surface2, color: C.text, border: `1px solid ${C.line}`, borderRadius: 4, fontSize: 10 }}>
            <option value="">-- Aucun stade --</option>
            {['a_contacter','rdv1','rdv2','rdv3','converti','perdu'].map(s => (
              <option key={s} value={s}>{s}</option>
            ))}
          </select>
        </div>
        {/* Auto-trigger toggle */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
          <span style={{ fontSize: 9, color: C.textLo }}>Auto</span>
          <Toggle checked={t.auto_trigger} onChange={() => toggleAutoTrigger(t)} />
          {/* Expand/collapse steps */}
          <SetBtn color={C.indigo} bg="#0d1a2e" onClick={() => setExpandedId(expandedId === t.id ? null : t.id)}>
            {expandedId === t.id ? '▲ Étapes' : '▼ Étapes'}
          </SetBtn>
          {/* Delete template */}
          <SetBtn color={C.cyan} bg="#1f0d0d" onClick={() => deleteTemplate(t.id)}>🗑️</SetBtn>
        </div>
      </SetRow>

      {/* Steps — visibles si expanded */}
      {expandedId === t.id && (
        <div style={{ padding: '8px 12px', background: C.bgMid }}>
          {t.steps.map(step => (
            <div key={step.id} style={{ display: 'flex', gap: 8, alignItems: 'center', marginBottom: 6 }}>
              {/* step_order badge */}
              <span style={{ fontSize: 9, color: C.gold, width: 20, textAlign: 'center' }}>J+{step.delay_days}</span>
              {/* Channel select */}
              <select value={step.channel}
                onChange={e => patchStep(step.id, { channel: e.target.value })}
                style={{ background: C.surface2, color: C.text, border: `1px solid ${C.line}`, borderRadius: 4, fontSize: 10 }}>
                {['whatsapp','email','sms','call_reminder','linkedin'].map(c => <option key={c} value={c}>{c}</option>)}
              </select>
              {/* delay_days input */}
              <input type="number" value={step.delay_days} min={0} max={365}
                onChange={e => patchStep(step.id, { delay_days: Number(e.target.value) })}
                style={{ width: 50, background: C.surface2, color: C.gold, border: `1px solid ${C.line}`, borderRadius: 4, fontSize: 10, textAlign: 'center' }} />
              <span style={{ fontSize: 9, color: C.textLo }}>jours</span>
              {/* Delete step */}
              <SetBtn color={C.cyan} bg="#1f0d0d" onClick={() => deleteStep(t.id, step.id)}>✕</SetBtn>
            </div>
          ))}
          {/* Ajouter step */}
          <SetBtn color={C.green} bg="#0d1a0d" onClick={() => addStep(t.id)}>+ Étape</SetBtn>
        </div>
      )}
    </div>
  ))}

  {error && <div style={{ color: C.cyan, fontSize: 10, padding: 8 }}>{error}</div>}
</SectionPanel>
```

**Comportements attendus :**
- `toggleAutoTrigger` : si l'API répond 409, afficher le message d'erreur dans `error` state (pas de crash)
- `addStep` : créer step avec `{ channel: 'email', delay_days: 3, step_order: t.steps.length + 1 }` puis recharger les steps du template
- `patchStep` : debounce optionnel (simple setState + PATCH onBlur sur les inputs texte)
- Tous les styles : `style={{ ... }}` avec `C.*` — jamais de classes Tailwind directes

**Note :** Les templates sont chargés sans les steps au départ (GET templates = leger). Les steps sont chargés uniquement quand l'utilisateur expand un template (`setExpandedId`). Lors du chargement des steps, les stocker dans `templates` state (remplacer l'objet template par `{ ...t, steps: loadedSteps }`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- `tsc --noEmit` sans erreur
- Onglet Séquences visible dans la page settings (plus de placeholder)
- Cliquer "+ Nouveau template" → champ input apparaît → Créer → template visible dans la liste
- Expand un template → steps visibles (vide si nouveau)
- "+ Étape" → step email J+3 ajoutée
- Supprimer un template → disparaît de la liste
- Toggle auto_trigger sur un stage déjà occupé → message d'erreur en rouge (pas de crash)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/crm/sequences/templates/* | Corps non fiable — IDs, enums, nombres |
| Route → Supabase | IDOR : .eq('user_id', user.id) sur toutes les queries templates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03B-01 | Tampering | IDOR template — PATCH/DELETE template d'un autre user | mitigate | `.eq('user_id', user.id)` sur toutes les queries + RLS |
| T-03B-02 | Tampering | IDOR step — modifier step appartenant à un autre template | mitigate | Vérifier `template.user_id = user.id` avant PATCH/DELETE step |
| T-03B-03 | Spoofing | auto_trigger collision deux templates même stage | mitigate | Conflict check 409 avant PATCH auto_trigger=true |
| T-03B-04 | Tampering | Injection via message_template text | accept | Stocké tel quel — interpolé avec `{{var}}` replace côté serveur Phase 5, pas de SQL dynamique |
| T-03B-05 | Tampering | step_order UNIQUE violation | mitigate | Gérer code '23505' PostgreSQL → apiError 409 clair |
| T-03B-06 | Spoofing | params.id non-UUID | accept | Supabase retourne 0 rows — pas de crash, apiError explicite |
</threat_model>

<verification>
- `npx tsc --noEmit` : 0 erreur
- Smoke test API : `POST /api/crm/sequences/templates` → 201
- Smoke test API : `PATCH /api/crm/sequences/templates/{id}` avec auto_trigger collision → 409
- UI : onglet Séquences opérationnel (templates, steps, toggles)
- UI : erreur 409 affichée inline — pas de page blanche
</verification>

<success_criteria>
- CFG-01 : templates CRUD fonctionnel (créer, modifier, supprimer)
- CFG-02 : delay_days configurable par step
- CFG-03 : channel configurable par step (whatsapp/email/sms/call_reminder/linkedin)
- CFG-05 : pipeline_stage + auto_trigger configurables par template
- CFG-08 : toggle auto_trigger individuel — conflit 409 bloquant
- CFG-09 : données rechargées depuis Supabase au montage du composant
- 0 erreur TypeScript
</success_criteria>

<output>
Après exécution, créer `.planning/phases/03-configuration/03-02-SUMMARY.md`
</output>
